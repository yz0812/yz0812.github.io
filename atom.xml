<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://wuc0714.top</id>
    <title>wuc0714</title>
    <updated>2021-05-09T16:22:06.296Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://wuc0714.top"/>
    <link rel="self" href="https://wuc0714.top/atom.xml"/>
    <subtitle>The more you know, the more you don&apos;t know</subtitle>
    <logo>https://wuc0714.top/images/avatar.png</logo>
    <icon>https://wuc0714.top/favicon.ico</icon>
    <rights>All rights reserved 2021, wuc0714</rights>
    <entry>
        <title type="html"><![CDATA[feign远程调用]]></title>
        <id>https://wuc0714.top/post/feign-yuan-cheng-diao-yong/</id>
        <link href="https://wuc0714.top/post/feign-yuan-cheng-diao-yong/">
        </link>
        <updated>2021-05-09T16:12:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="一feign远程调用">一.feign远程调用</h2>
<h3 id="1导入包">1.导入包</h3>
<pre><code class="language-xml">   &lt;dependency&gt;
            &lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;
            &lt;artifactId&gt;spring-cloud-starter-openfeign&lt;/artifactId&gt;
   &lt;/dependency&gt;
</code></pre>
<h3 id="2加上注解enablefeignclients">2.加上注解@EnableFeignClients</h3>
<h3 id="3编写client">3.编写client</h3>
<pre><code class="language-java">//name 服务名
@FeignClient(name = &quot;spring-cloud-alibaba-nacos&quot;)
public interface UserCenterFeignClient {
    @GetMapping(&quot;/user/{id}&quot;)
     String user(@PathVariable String id) ;
}

</code></pre>
<h1 id="二feign细粒度配置自定义-代码方式">二.feign细粒度配置自定义-代码方式</h1>
<table>
<thead>
<tr>
<th>级别</th>
<th>打印内容</th>
</tr>
</thead>
<tbody>
<tr>
<td>NONE(默认值)</td>
<td>不记录任何日志</td>
</tr>
<tr>
<td>BASIC(适合生产环境使用)</td>
<td>仅记录请求方法,URL,响应代码以及执行时间</td>
</tr>
<tr>
<td>HEADERS</td>
<td>记录BASIC级别的基础上,记录请求和响应的header</td>
</tr>
<tr>
<td>FULL</td>
<td>记录请求和响应的header,body元数据</td>
</tr>
</tbody>
</table>
<h3 id="1编写配置类">1.编写配置类</h3>
<pre><code class="language-java">/**
 * feign的配置类
 * 这个类别加@configuration,否则必须挪到@ComponentScan扫描不到的包，及application意外险
 * 否则父子上下文重叠
 */
public class UserCenterFeignConfiguration {
    @Bean
    public Logger.Level level(){
        //让feign打印所有请求
       return  Logger.Level.FULL;
    }
}
</code></pre>
<h3 id="2feignclient注解增加配置">2.@FeignClient注解增加配置</h3>
<pre><code class="language-java">@FeignClient(name = &quot;spring-cloud-alibaba-nacos&quot;,configuration = UserCenterFeignConfiguration.class)
public interface UserCenterFeignClient {
    @GetMapping(&quot;/user/{id}&quot;)
     String user(@PathVariable String id) ;
}
</code></pre>
<h3 id="3修改日志级别">3.修改日志级别</h3>
<pre><code class="language-yaml">logging:
  level:
    com.yz.alibaba.feignclient.UserCenterFeignClient:  debug
</code></pre>
<h1 id="二feign细粒度配置自定义-属性配置">二.feign细粒度配置自定义-属性配置</h1>
<p>修改配置</p>
<pre><code class="language-yaml">logging:
  level:
    com.yz.alibaba.feignclient.UserCenterFeignClient:  debug
feign:
  client:
    config:
      spring-cloud-alibaba-nacos:
        loggerLevel: full
</code></pre>
<h1 id="三feign细粒度配置自定义-全局配置">三.feign细粒度配置自定义-全局配置</h1>
<ol>
<li>
<p>修改启动类注解<span style="color:red">@EnableFeignClients(defaultConfiguration = UserCenterFeignConfiguration.class)</span> UserCenterFeignConfiguration为自定义配置类</p>
</li>
<li>
<p>修改配置文件</p>
</li>
</ol>
<pre><code>logging:
  level:
    com.yz.alibaba.feignclient.UserCenterFeignClient:  debug
feign:
  client:
    config:
      default:
        loggerLevel: full
</code></pre>
<h1 id="四支持的配置项">四.支持的配置项</h1>
<pre><code class="language-java">private Level loggerLevel;       //日志级别
private Integer connectTimeout;  //链接超时时间
private Integer readTimeout;     //读取超时时间
private Class&lt;Retryer&gt; retryer;  //重试策略
private Class&lt;ErrorDecoder&gt; errorDecoder; //错误解码器
private List&lt;Class&lt;RequestInterceptor&gt;&gt; requestInterceptors; //拦截器
private Boolean decode404; //是否对404错误码解码
private Class&lt;Decoder&gt; decoder; //解码器
private Class&lt;Encoder&gt; encoder; //编码器
private Class&lt;Contract&gt; contract; //契约
private ExceptionPropagationPolicy exceptionPropagationPolicy; //异常传播策略
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/yz0812/mypic/raw/master/20210507011811.png" alt="" loading="lazy"></figure>
<h1 id="五代码配置属性配置对比">五.代码配置,属性配置对比</h1>
<table>
<thead>
<tr>
<th>配置方式</th>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody>
<tr>
<td>代码方式</td>
<td>基于代码,更加灵活</td>
<td>如果Feign的配置类加了@Configuration注解,需要注意父子上下文,<br/>线上修改得重新打包</td>
</tr>
<tr>
<td>属性配置</td>
<td>易上手<br>配置更加直观<br/>线上修改无需重新打包发布,优先级更高</td>
<td>极端场景下,没有代码配置方式更加灵活</td>
</tr>
</tbody>
</table>
<h1 id="六feignclient注解异常">六.@FeignClient注解异常</h1>
<pre><code class="language-java">Description:The bean 'spring-cloud-alibaba-nacos.FeignClientSpecification' could not be registered. A bean with that name has already been defined and overriding is disabled.Action:Consider renaming one of the beans or enabling overriding by setting spring.main.allow-bean-definition-overriding=true
</code></pre>
<p>@FeignClient name重复,在配置文件增加配置<span style="color:red">spring.main.allow-bean-definition-overriding=true</span></p>
<h1 id="七feign继承">七.feign继承</h1>
<p>spring官方不推荐使用,不符合微服务特性,但是挺多公司使用,一致性,方便</p>
<p>使用方式</p>
<p>公共访问接口-&gt; 服务提供者实现公共访问接口-&gt; feign继承公共访问接口-&gt;接口调用feign</p>
<h4 id="1-公共访问接口-公共服务类">1. 公共访问接口-公共服务类</h4>
<pre><code class="language-java">@RequestMapping(&quot;/hs2&quot;)public interface InheritanceService {    @RequestMapping(value = &quot;/hello1&quot;, method = RequestMethod.GET)    String hello(@RequestParam(&quot;name&quot;) String name);    @RequestMapping(value = &quot;/hello2&quot;, method = RequestMethod.GET)    Book hello(@RequestHeader(&quot;name&quot;) String name, @RequestHeader(&quot;author&quot;) String author, @RequestHeader(&quot;price&quot;) Integer price) throws UnsupportedEncodingException;    @RequestMapping(value = &quot;/hello3&quot;, method = RequestMethod.POST)    String hello(@RequestBody Book book);}
</code></pre>
<h4 id="2服务提供者实现公共访问接口-服务a">2.服务提供者实现公共访问接口-服务A</h4>
<pre><code class="language-java">@RestControllerpublic class InheritanceFeignController implements InheritanceService {    @Override    public String hello(@RequestParam(&quot;name&quot;) String name) {        return &quot;hello &quot; + name + &quot;!&quot;;    }    @Override    public Book hello(@RequestHeader(&quot;name&quot;) String name, @RequestHeader(&quot;author&quot;) String author, @RequestHeader(&quot;price&quot;) Integer price) throws UnsupportedEncodingException {      Book book = new Book();        book.setName(URLDecoder.decode(name,&quot;UTF-8&quot;));        book.setAuthor(URLDecoder.decode(author,&quot;UTF-8&quot;));        book.setPrice(price);        System.out.println(book);        return book;    }    @Override    public String hello(@RequestBody Book book) {        return &quot;书名为：&quot; + book.getName() + &quot;;作者为：&quot; + book.getAuthor();    }}
</code></pre>
<h4 id="3feign继承公共访问接口-服务b">3.feign继承公共访问接口-服务B</h4>
<pre><code class="language-java">@FeignClient(&quot;spring-cloud-alibaba-nacos&quot;)public interface InheritanceFeignClient extends InheritanceService {}
</code></pre>
<h4 id="4接口调用feign-服务b">4.接口调用feign-服务B</h4>
<pre><code class="language-java">@RestController@RequiredArgsConstructor(onConstructor = @_(@Autowired))public class InheritanceFeignController {    private final InheritanceService inheritanceService;    @RequestMapping(&quot;/hello1&quot;)    public String hello1() {        return inheritanceService.hello(&quot;张三&quot;);    }    @RequestMapping(value = &quot;/hello2&quot;)    public Book hello2() throws UnsupportedEncodingException {        Book book = inheritanceService.hello(URLEncoder.encode(&quot;三国演义&quot;,&quot;UTF-8&quot;), URLEncoder.encode(&quot;罗贯中&quot;,&quot;UTF-8&quot;), 33);        System.out.println(book);        return book;    }    @RequestMapping(&quot;/hello3&quot;)    public String hello3() {        Book book = new Book();        book.setName(&quot;红楼梦&quot;);        book.setPrice(44);        book.setAuthor(&quot;曹雪芹&quot;);        return inheritanceService.hello(book);    }}
</code></pre>
<h1 id="八如何使用feign构造多参数的请求">八.如何使用Feign构造多参数的请求</h1>
<h2 id="get请求多参数的url">GET请求多参数的URL</h2>
<p>假设需请求的URL包含多个参数，例如<code>http://microservice-provider-user/get?id=1&amp;username=张三</code> ，该如何使用Feign构造呢？</p>
<p>我们知道，Spring Cloud为Feign添加了Spring MVC的注解支持，那么我们不妨按照Spring MVC的写法尝试一下：</p>
<pre><code class="language-java">@FeignClient(&quot;microservice-provider-user&quot;)public interface UserFeignClient {  @RequestMapping(value = &quot;/get&quot;, method = RequestMethod.GET)  public User get0(User user);}
</code></pre>
<p>然而，这种写法并不正确，控制台会输出类似如下的异常。</p>
<pre><code class="language-shell">feign.FeignException: status 405 reading UserFeignClient#get0(User); content:{&quot;timestamp&quot;:1482676142940,&quot;status&quot;:405,&quot;error&quot;:&quot;Method Not Allowed&quot;,&quot;exception&quot;:&quot;org.springframework.web.HttpRequestMethodNotSupportedException&quot;,&quot;message&quot;:&quot;Request method 'POST' not supported&quot;,&quot;path&quot;:&quot;/get&quot;}
</code></pre>
<p>由异常可知，尽管我们指定了GET方法，Feign依然会使用POST方法发送请求。于是导致了异常。正确写法如下</p>
<h3 id="方法一推荐">方法一[推荐]</h3>
<pre><code class="language-java">@FeignClient(&quot;microservice-provider-user&quot;)public interface UserFeignClient {  @GetMapping(&quot;/get&quot;)  public User get0(@SpringQueryMap User user);}
</code></pre>
<h3 id="方法二推荐">方法二[推荐]</h3>
<pre><code class="language-java">@FeignClient(name = &quot;microservice-provider-user&quot;)public interface UserFeignClient {  @RequestMapping(value = &quot;/get&quot;, method = RequestMethod.GET)  public User get1(@RequestParam(&quot;id&quot;) Long id, @RequestParam(&quot;username&quot;) String username);}
</code></pre>
<p>这是最为直观的方式，URL有几个参数，Feign接口中的方法就有几个参数。使用@RequestParam注解指定请求的参数是什么。</p>
<h3 id="方法三不推荐">方法三[不推荐]</h3>
<p>多参数的URL也可使用Map来构建。当目标URL参数非常多的时候，可使用这种方式简化Feign接口的编写。</p>
<pre><code class="language-java">@FeignClient(name = &quot;microservice-provider-user&quot;)public interface UserFeignClient {  @RequestMapping(value = &quot;/get&quot;, method = RequestMethod.GET)  public User get2(@RequestParam Map&lt;String, Object&gt; map);}
</code></pre>
<p>在调用时，可使用类似以下的代码。</p>
<pre><code class="language-java">public User get(String username, String password) {  HashMap&lt;String, Object&gt; map = Maps.newHashMap();  map.put(&quot;id&quot;, &quot;1&quot;);  map.put(&quot;username&quot;, &quot;张三&quot;);  return this.userFeignClient.get2(map);}
</code></pre>
<p><strong>注意</strong>：这种方式不建议使用。主要是因为可读性不好，而且如果参数为空的时候会有一些问题，例如<code>map.put(&quot;username&quot;, null);</code> 会导致<code>microservice-provider-user</code> 服务接收到的username是<code>&quot;&quot;</code> ，而不是null。</p>
<h2 id="post请求包含多个参数">POST请求包含多个参数</h2>
<p>下面来讨论如何使用Feign构造包含多个参数的POST请求。假设服务提供者的Controller是这样编写的：</p>
<pre><code class="language-java">@RestControllerpublic class UserController {  @PostMapping(&quot;/post&quot;)  public User post(@RequestBody User user) {    ...  }}
</code></pre>
<p>我们要如何使用Feign去请求呢？答案非常简单，示例：</p>
<pre><code class="language-java">@FeignClient(name = &quot;microservice-provider-user&quot;)public interface UserFeignClient {  @RequestMapping(value = &quot;/post&quot;, method = RequestMethod.POST)  public User post(@RequestBody User user);}
</code></pre>
<h1 id="九resttemplate-vs-feign">九.RestTemplate VS Feign</h1>
<table>
<thead>
<tr>
<th>角度</th>
<th>RestTemplate</th>
<th>Feign</th>
</tr>
</thead>
<tbody>
<tr>
<td>可读性,可维护性</td>
<td>一般</td>
<td>极佳</td>
</tr>
<tr>
<td>开发体验</td>
<td>欠佳</td>
<td>极佳</td>
</tr>
<tr>
<td>性能</td>
<td>很好</td>
<td>中等</td>
</tr>
<tr>
<td>灵活性</td>
<td>极佳</td>
<td>中等</td>
</tr>
</tbody>
</table>
<h1 id="十feign线程池">十.feign线程池</h1>
<p>导入包</p>
<pre><code class="language-xml">&lt;dependency&gt;    &lt;groupId&gt;io.github.openfeign&lt;/groupId&gt;    &lt;artifactId&gt;feign-httpclient&lt;/artifactId&gt;&lt;/dependency&gt;
</code></pre>
<p>还可以使用</p>
<pre><code class="language-xml">&lt;dependency&gt;    &lt;groupId&gt;io.github.openfeign&lt;/groupId&gt;    &lt;artifactId&gt;feign-okhttp&lt;/artifactId&gt;&lt;/dependency&gt;
</code></pre>
<pre><code class="language-yaml">feign:  #okhttp   httpclient:    #是否开启    enabled: true    #最大连接数    max-connections: 200    #最大路由    max-connections-per-route: 50    #存活时间    time-to-live: 900    #存活时间单位    time-to-live-unit: minutes    #跟随重定向    follow-redirects: true    #连接超时时间    connection-timeout: 2000    #超时重试时间    connection-timer-repeat: 3000
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[扩展ribbon,支持naocs权重]]></title>
        <id>https://wuc0714.top/post/kuo-zhan-ribbonzhi-chi-naocs-quan-chong/</id>
        <link href="https://wuc0714.top/post/kuo-zhan-ribbonzhi-chi-naocs-quan-chong/">
        </link>
        <updated>2021-05-09T16:11:29.000Z</updated>
        <content type="html"><![CDATA[<p>÷编写代码</p>
<pre><code class="language-java">import com.alibaba.cloud.nacos.NacosDiscoveryProperties;
import com.alibaba.cloud.nacos.ribbon.NacosServer;
import com.alibaba.nacos.api.exception.NacosException;
import com.alibaba.nacos.api.naming.NamingService;
import com.alibaba.nacos.api.naming.pojo.Instance;
import com.netflix.client.config.IClientConfig;
import com.netflix.loadbalancer.AbstractLoadBalancerRule;
import com.netflix.loadbalancer.BaseLoadBalancer;
import com.netflix.loadbalancer.Server;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;

@Slf4j
public class NacosWeightedRule extends AbstractLoadBalancerRule {
    @Autowired
    private NacosDiscoveryProperties nacosDiscoveryProperties;

    @Override
    public void initWithNiwsConfig(IClientConfig iClientConfig) {
        //读取配置文件，并且初始化
    }

    @Override
    public Server choose(Object o) {
        try {
            BaseLoadBalancer loadBalancer = (BaseLoadBalancer) this.getLoadBalancer();
            //获得服务名
            String name = loadBalancer.getName();
            //拿到服务相关的api
            NamingService namingService = nacosDiscoveryProperties.namingServiceInstance();
            //nacos client自动通过基于权重的负载均衡算法，给我们选择一个实例
            Instance instant = namingService.selectOneHealthyInstance(name);
            log.info(&quot;选择的实例是：prot={},instant={}&quot;, instant.getPort(), instant);
            return new NacosServer(instant);
        } catch (NacosException e) {
            e.printStackTrace();
            return null;
        }

    }
}
</code></pre>
<p>修改配置类</p>
<pre><code class="language-java">@Configuration
public class RibbonConfiguration {
    @Bean
    public IRule ribbonRule() {
        return new NacosWeightedRule();
    }
}

</code></pre>
<p>在配置文件中使用</p>
<pre><code class="language-yaml">spring-cloud-alibaba-nacos:
  ribbon:
    NFLoadBalancerRuleClassName: com.yz.alibaba.config.NacosWeightedRule
</code></pre>
<p>修改nacos权重<br>
<img src="https://gitee.com/yz0812/mypic/raw/master/20210505221219.png" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ribbon自定义负载均衡策略]]></title>
        <id>https://wuc0714.top/post/ribbon-zi-ding-yi-fu-zai-jun-heng-ce-lue/</id>
        <link href="https://wuc0714.top/post/ribbon-zi-ding-yi-fu-zai-jun-heng-ce-lue/">
        </link>
        <updated>2021-05-09T16:10:02.000Z</updated>
        <content type="html"><![CDATA[<p>细粒度配置自定义,java代码实现</p>
<pre><code class="language-java">/**
*必须放在启动类外面的包,不然会被启动类扫描到,出现父子上下文重叠
*/
@Configuration
public class RibbonConfiguration {
    @Bean
    public IRule ribbonRule(){
        return new RandomRule();
    }
}
</code></pre>
<pre><code class="language-java">package com.yz.alibaba.config;

import com.yz.configuration.RibbonConfiguration;
import org.springframework.cloud.netflix.ribbon.RibbonClient;
import org.springframework.context.annotation.Configuration;

@Configuration
@RibbonClient(name = &quot;spring-cloud-alibaba-nacos&quot;,configuration = RibbonConfiguration.class)
//全局配置
//@RibbonClients(defaultConfiguration = RibbonConfiguration.class)
public class UserRibbonConfiguration {
}

</code></pre>
<p>配置文件实现</p>
<pre><code class="language-yaml">#服务名
spring-cloud-alibaba-nacos:
  ribbon:
    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule
</code></pre>
<table>
<thead>
<tr>
<th><em><strong>配置方式</strong></em></th>
<th><em><strong>优点</strong></em></th>
<th><em><strong>缺点</strong></em></th>
</tr>
</thead>
<tbody>
<tr>
<td>代码配置</td>
<td>基于代码更加灵活</td>
<td>有小坑(父子上下文),线上修改得重新打包,发布</td>
</tr>
<tr>
<td>属性配置</td>
<td>易上手,配置更加直观,线上发布无需重新打包发布,优先级更高</td>
<td>极端场景下没有代码配置方式灵活</td>
</tr>
</tbody>
</table>
<p>ribbon的策略主要有以下几种:</p>
<ul>
<li>com.netflix.loadbalancer.RandomRule #配置规则 随机</li>
<li>com.netflix.loadbalancer.RoundRobinRule #配置规则 轮询</li>
<li>com.netflix.loadbalancer.RetryRule #配置规则 重试</li>
<li>com.netflix.loadbalancer.WeightedResponseTimeRule #配置规则 响应时间权重</li>
<li>com.netflix.loadbalancer.BestAvailableRule #配置规则 最空闲连接策略</li>
</ul>
<p>随机:几个提供者间随机访问</p>
<p>轮询:轮流访问</p>
<p>重试:在一段时间内通过RoundRobinRule选择服务实例，一段时间内没有选择出服务则线程终止</p>
<p>RestTemplate 第一次调用时间比较久,所以使用饥饿加载</p>
<pre><code class="language-java">ribbon:
  eager-load:
    clients: spring-cloud-alibaba-nacos
    enabled: true
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[springcloud配置文件]]></title>
        <id>https://wuc0714.top/post/springcloud-pei-zhi-wen-jian/</id>
        <link href="https://wuc0714.top/post/springcloud-pei-zhi-wen-jian/">
        </link>
        <updated>2021-05-03T10:17:58.000Z</updated>
        <content type="html"><![CDATA[<pre><code class="language-yaml"># 公用环境
spring:
    application:
        name: spring-cloud-alibbaba
# 连字符
--- 
spring:
  profiles: dev
---
spring:
  profiles: pro
management:
  endpoint:
    health:
        show-details: always
  endpoints:
    web:
      exposure:
          include: '*'
</code></pre>
<p>可以在一个配置文件写多个环境的配置</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[docker安装的jenkins升级]]></title>
        <id>https://wuc0714.top/post/jenkins-sheng-ji/</id>
        <link href="https://wuc0714.top/post/jenkins-sheng-ji/">
        </link>
        <updated>2021-04-24T14:58:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="1更换安装源">1.更换安装源</h3>
<p><strong>jenkins 更换安装源</strong>,进入<code>http://127.0.0.1:8091/pluginManager/advanced</code>,高级设置</p>
<p>修改站点为清华源地址<code>https://mirrors.tuna.tsinghua.edu.cn/jenkins/updates/update-center.json</code></p>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/yz0812/mypic/raw/master/20210424204159.png" alt="" loading="lazy"></figure>
<h3 id="2修改下载地址">2.修改下载地址</h3>
<p>进入/jenkins/updates/ 找到default.json</p>
<p>这是老版本的替换命令，具体来说就是将<code>updates.jenkins-ci.org</code>替换为<code>mirrors.tuna.tsinghua.edu.cn</code>，<code>www.google.com</code>替换为<code>www.baidu.com</code>：</p>
<pre><code class="language-sh">sed -i 's#http://updates.jenkins-ci.org/download#https://mirrors.tuna.tsinghua.edu.cn/jenkins#g' default.json &amp;&amp; sed -i 's#http://www.google.com#https://www.baidu.com#g' default.json
</code></pre>
<p>当然，在最新的jenkins版本中，已经有了调整，配置文件中的地址不再是<code>updates.jenkins-ci.org</code> ,而是将<code>https://updates.jenkins.io/download</code></p>
<p>替换为<code>https://mirrors.tuna.tsinghua.edu.cn/jenkins</code></p>
<pre><code class="language-shell">sed -i 's#https://updates.jenkins.io/download#https://mirrors.tuna.tsinghua.edu.cn/jenkins#g' default.json &amp;&amp; sed -i 's#http://www.google.com#https://www.baidu.com#g' default.json
</code></pre>
<h3 id="3jenkins更新war包">3.jenkins更新war包</h3>
<p>1.查询container_id</p>
<pre><code class="language-docker">docker container ls
</code></pre>
<p>2.进入容器,使用root身份</p>
<pre><code class="language-sh">docker exec -it -u root [container_id] /bin/bash
</code></pre>
<p>3.其次，执行命令查看java进程</p>
<pre><code class="language-shell">root@dd7cdfba9d52:/# ps -ef | grep java
jenkins      8     1  1 19:57 ?        00:01:15 java -Duser.home=/var/jenkins_home -Djenkins.model.Jenkins.slaveAgentPort=50000 -jar /usr/share/jenkins/jenkins.war
root       103    95  0 21:02 pts/0    00:00:00 grep java
root@dd7cdfba9d52:/# 
</code></pre>
<p>4.下载最新的war包</p>
<pre><code class="language-shell"># 进入容器之后进入进入war存放地址
cd /usr/share/jenkins/
# 将之前的war修改名字
cp jenkins.war jenkins.war.2.60.3.bak
# 创建文件夹用于存放新下载的war
mkdir other-version
# 进入文件夹
cd other-version/
# 下载新的war
wget https://updates.jenkins.io/download/war/2.289/jenkins.war
# 进入存放目录
cd /usr/share/jenkins
# 删除旧war
rm jenkins.war
# 进入新的文件夹
cd other-version
# 移动war
mv jenkins.war ../
# 退出
exit
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://gitee.com/yz0812/mypic/raw/master/5C7C2708-33CE-4EE3-8B00-850496E27E7E.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[微信小程序使用 Eslint]]></title>
        <id>https://wuc0714.top/post/wei-xin-xiao-cheng-xu-shi-yong-eslint/</id>
        <link href="https://wuc0714.top/post/wei-xin-xiao-cheng-xu-shi-yong-eslint/">
        </link>
        <updated>2021-04-20T15:37:11.000Z</updated>
        <content type="html"><![CDATA[<p>原生微信小程序安装Eslint</p>
<h4 id="1-vscode安装eslint插件">1. VsCode安装Eslint插件</h4>
<h4 id="2-cd到我们的项目安装eslint">2. cd到我们的项目，安装ESlint</h4>
<pre><code class="language-shell">eslintrc.jscnpm install eslint --save-dev
</code></pre>
<h4 id="3-创建eslint配置文件eslintrcjs">3. 创建ESlint配置文件.eslintrc.js</h4>
<pre><code class="language-shell">npx eslint --init
#或者
eslint --init
</code></pre>
<h4 id="4-eslintrcjs文件生成之后我们就可以使用基本的命令规范我们的代码了">4. eslintrc.js文件生成之后，我们就可以使用基本的命令规范我们的代码了</h4>
<pre><code class="language-shell">eslint --fix pages/index/index.js
</code></pre>
<h4 id="当然如果你使用了es6的语法上述命令执行后会报如下错误">当然，如果你使用了ES6的语法，上述命令执行后会报如下错误</h4>
<pre><code class="language-js">Parsing error: Unexpected token = ***
</code></pre>
<h4 id="原因是开发环境与eslint当前的解析功能不兼容因此我们需要执行下一个步骤">原因是开发环境与ESLint当前的解析功能不兼容，因此我们需要执行下一个步骤；</h4>
<pre><code class="language-js">cnpm install babel-eslint --save-dev
</code></pre>
<h4 id="安装完成后在eslintrc中添加配置parser-babel-eslint-即可">安装完成后，在.eslintrc中添加配置：&quot;parser&quot;: &quot;babel-eslint&quot; 即可；</h4>
<h4 id="5-eslintrcjs参考">5. eslintrc.js参考</h4>
<pre><code class="language-javascript">module.exports = {
  &quot;env&quot;: {
    &quot;browser&quot;: true,
    &quot;node&quot;: true,
    &quot;es6&quot;: true, //使用require就不会报错了
    &quot;commonjs&quot;: true
  },
  //配置解析器
  &quot;parser&quot;: &quot;babel-eslint&quot;,
  &quot;parserOptions&quot;: {
    &quot;ecmaVersion&quot;: 6,
    &quot;sourceType&quot;: &quot;module&quot;,
  },

  &quot;extends&quot;: &quot;eslint:recommended&quot;,
  //全局变量
  &quot;globals&quot;: {
    &quot;__DEV__&quot;: true,
    &quot;__WECHAT__&quot;: true,
    &quot;__ALIPAY__&quot;: true,
    &quot;App&quot;: true,
    &quot;Page&quot;: true,
    &quot;Component&quot;: true,
    &quot;Behavior&quot;: true,
    &quot;wx&quot;: true,
    &quot;getApp&quot;: true,
    &quot;getCurrentPages&quot;: true,
  },
  /**
   * &quot;off&quot; 或 0 - 关闭规则
   * &quot;warn&quot; 或 1 - 开启规则，使用警告级别的错误：warn (不会导致程序退出),
   * &quot;error&quot; 或 2 - 开启规则，使用错误级别的错误：error (当被触发的时候，程序会退出)
   */
  &quot;rules&quot;: {
    // 强制使用一致的缩进
    &quot;indent&quot;: [
      &quot;error&quot;, 2
    ],
    // 强制使用一致的换行风格
    &quot;linebreak-style&quot;: [
      &quot;error&quot;,
      &quot;unix&quot;
    ],
    // 强制使用一致的反勾号、双引号或单引号
    &quot;quotes&quot;: [
      &quot;error&quot;,
      &quot;single&quot; // backtick、double、single
    ],
    // 要求或禁止使用分号代替 ASI
    &quot;semi&quot;: [
      &quot;error&quot;,
      &quot;always&quot;
    ],
    &quot;no-console&quot;: [
      &quot;off&quot;, {
        &quot;allow&quot;: [&quot;log&quot;, &quot;warn&quot;, &quot;error&quot;, &quot;info&quot;] // &quot;allow&quot; 是个字符串数组，包含允许使用的console 对象的方法
      }
    ],
    //禁止空格和 tab 的混合缩进
    &quot;no-mixed-spaces-and-tabs&quot;: [
      &quot;error&quot;, &quot;smart-tabs&quot;
    ],
    // 强制在 function的左括号之前使用一致的空格
    &quot;space-before-function-paren&quot;: [2, &quot;always&quot;],
    // 禁止或强制在单行代码块中使用空格(禁用)
    &quot;block-spacing&quot;: [1, &quot;never&quot;],

    &quot;no-cond-assign&quot;: 2,
    // 禁止 function 定义中出现重名参数
    &quot;no-dupe-args&quot;: 2,
    // 禁止对象字面量中出现重复的 key
    &quot;no-dupe-keys&quot;: 2,
    // 禁止重复的 case 标签
    &quot;no-duplicate-case&quot;: 2,
    // 禁止空语句块
    &quot;no-empty&quot;: 2,
    // 禁止对 catch 子句的参数重新赋值
    &quot;no-ex-assign&quot;: 2,
    // 禁止不必要的布尔转换
    &quot;no-extra-boolean-cast&quot;: 2,
    // 禁止不必要的括号 //(a * b) + c;//报错
    &quot;no-extra-parens&quot;: 0,

    //强制所有控制语句使用一致的括号风格
    &quot;curly&quot;: [2, &quot;all&quot;],
    // 禁止 catch 子句的参数与外层作用域中的变量同名
    &quot;no-catch-shadow&quot;: 0,
    // 不允许标签与变量同名
    &quot;no-label-var&quot;: 2,
    // 禁用特定的全局变量
    &quot;no-restricted-globals&quot;: 2,
    // 禁止 var 声明 与外层作用域的变量同名
    &quot;no-shadow&quot;: 0,
    // 禁止覆盖受限制的标识符
    &quot;no-shadow-restricted-names&quot;: 2,
    // 禁止将变量初始化为 undefined
    &quot;no-undef-init&quot;: 2,
    // 禁止将 undefined 作为标识符
    &quot;no-undefined&quot;: 0,
    // 不允许在变量定义之前使用它们
    &quot;no-use-before-define&quot;: 0,
    //////////////
    // 风格指南 //
    //////////////
    // 指定数组的元素之间要以空格隔开(, 后面)， never参数：[ 之前和 ] 之后不能带空格，always参数：[ 之前和 ] 之后必须带空格
    &quot;array-bracket-spacing&quot;: [2, &quot;never&quot;],
    //强制使用一致的缩进 第二个参数为 &quot;tab&quot; 时，会使用tab，
    // if while function 后面的{必须与if在同一行，java风格。
    &quot;brace-style&quot;: [2, &quot;1tbs&quot;, {
      &quot;allowSingleLine&quot;: true
    }],
    // 控制逗号前后的空格
    &quot;comma-spacing&quot;: [2, {
      &quot;before&quot;: false,
      &quot;after&quot;: true
    }],
    // 控制逗号在行尾出现还是在行首出现 (默认行尾)
    // http://eslint.org/docs/rules/comma-style
    &quot;comma-style&quot;: [2, &quot;last&quot;],
    //&quot;SwitchCase&quot; (默认：0) 强制 switch 语句中的 case 子句的缩进水平
    // 以方括号取对象属性时，[ 后面和 ] 前面是否需要空格, 可选参数 never, always
    &quot;computed-property-spacing&quot;: [2, &quot;never&quot;],
    // 用于指统一在回调函数中指向this的变量名，箭头函数中的this已经可以指向外层调用者，应该没卵用了
    // e.g [0,&quot;self&quot;] 指定只能 var that = this. self不能指向其他任何值，this也不能赋值给self以外的其他值
    &quot;consistent-this&quot;: [2, &quot;self&quot;, &quot;that&quot;, &quot;_self&quot;, &quot;_that&quot;, &quot;me&quot;, &quot;_this&quot;],
    // 强制使用命名的 function 表达式
    &quot;func-names&quot;: 0,
    // 文件末尾强制换行
    &quot;eol-last&quot;: 0,
    //要求或禁止在函数标识符和其调用之间有空格
    &quot;func-call-spacing&quot;: 2,
    // 强制在对象字面量的属性中键和值之间使用一致的间距
    &quot;key-spacing&quot;: [2, {
      &quot;beforeColon&quot;: false,
      &quot;afterColon&quot;: true
    }],
    // 要求在注释周围有空行 ( 要求在块级注释之前有一空行)
    &quot;lines-around-comment&quot;: [2, {
      &quot;beforeBlockComment&quot;: true
    }],
    &quot;func-style&quot;: 0,
    // 强制回调函数最大嵌套深度 5层
    &quot;max-nested-callbacks&quot;: [2, 5],
    // 禁止使用指定的标识符
    &quot;id-blacklist&quot;: 0,
    // 强制标识符的最新和最大长度
    &quot;id-length&quot;: 0,
    // 要求标识符匹配一个指定的正则表达式
    &quot;id-match&quot;: 0,
    // 强制在 JSX 属性中一致地使用双引号或单引号
    &quot;jsx-quotes&quot;: 0,
    // 强制在关键字前后使用一致的空格 (前后腰需要)
    &quot;keyword-spacing&quot;: 2,
    // 强制最大行数
    &quot;max-lines&quot;: 0,
    // 强制 function 定义中最多允许的参数数量
    &quot;max-params&quot;: [1, 5],
    // 强制 function 块最多允许的的语句数量
    &quot;max-statements&quot;: [1, 200],
    // 强制每一行中所允许的最大语句数量
    &quot;max-statements-per-line&quot;: 0,
    // 要求构造函数首字母大写 （要求调用 new 操作符时有首字母大小的函数，允许调用首字母大写的函数时没有 new 操作符。）
    &quot;new-cap&quot;: [2, {
      &quot;newIsCap&quot;: true,
      &quot;capIsNew&quot;: false
    }],
    // 要求调用无参构造函数时有圆括号
    &quot;new-parens&quot;: 2,
    // 要求或禁止 var 声明语句后有一行空行
    &quot;newline-after-var&quot;: 0,
    // 禁止使用 Array 构造函数
    &quot;no-array-constructor&quot;: 2,
    // 禁用按位运算符
    &quot;no-bitwise&quot;: 0,
    // 要求 return 语句之前有一空行
    &quot;newline-before-return&quot;: 0,
    // 要求方法链中每个调用都有一个换行符
    &quot;newline-per-chained-call&quot;: 1,
    // 禁用 continue 语句
    &quot;no-continue&quot;: 0,
    // 禁止在代码行后使用内联注释
    &quot;no-inline-comments&quot;: 0,
    // 禁止 if 作为唯一的语句出现在 else 语句中
    &quot;no-lonely-if&quot;: 0,
    // 禁止混合使用不同的操作符
    &quot;no-mixed-operators&quot;: 0,
    // 不允许多个空行
    &quot;no-multiple-empty-lines&quot;: [2, {
      &quot;max&quot;: 2
    }],
    // 不允许否定的表达式
    &quot;no-negated-condition&quot;: 0,
    // 不允许使用嵌套的三元表达式
    &quot;no-nested-ternary&quot;: 0,
    // 禁止使用 Object 的构造函数
    &quot;no-new-object&quot;: 2,
    // 禁止使用一元操作符 ++ 和 --
    &quot;no-plusplus&quot;: 0,
    // 禁止使用特定的语法
    &quot;no-restricted-syntax&quot;: 0,
    // 禁止 function 标识符和括号之间出现空格
    &quot;no-spaced-func&quot;: 2,
    // 不允许使用三元操作符
    &quot;no-ternary&quot;: 0,
    // 禁用行尾空格
    &quot;no-trailing-spaces&quot;: 2,
    // 禁止标识符中有悬空下划线_bar
    &quot;no-underscore-dangle&quot;: 0,
    // 禁止可以在有更简单的可替代的表达式时使用三元操作符
    &quot;no-unneeded-ternary&quot;: 2,
    // 禁止属性前有空白
    &quot;no-whitespace-before-property&quot;: 2,
    // 要求或禁止在 var 声明周围换行
    &quot;one-var-declaration-per-line&quot;: 0,
    // 要求或禁止在可能的情况下要求使用简化的赋值操作符
    &quot;operator-assignment&quot;: 0,
    // 强制操作符使用一致的换行符
    &quot;operator-linebreak&quot;: [2, &quot;after&quot;, {
      &quot;overrides&quot;: {
        &quot;?&quot;: &quot;before&quot;,
        &quot;:&quot;: &quot;before&quot;
      }
    }],
    // 要求或禁止块内填充
    &quot;padded-blocks&quot;: 0,
    // 要求对象字面量属性名称用引号括起来
    &quot;quote-props&quot;: 0,
    // 要求使用 JSDoc 注释
    &quot;require-jsdoc&quot;: 0,
    // 要求或禁止使用分号而不是 ASI（这个才是控制行尾部分号的，）
    // &quot;semi&quot;: [2, &quot;always&quot;],
    // 强制分号之前和之后使用一致的空格
    &quot;semi-spacing&quot;: 2,
    // 要求同一个声明块中的变量按顺序排列
    &quot;sort-vars&quot;: 0,
    // 强制在块之前使用一致的空格
    &quot;space-before-blocks&quot;: [2, &quot;always&quot;],
    // 强制在圆括号内使用一致的空格
    &quot;space-in-parens&quot;: [2, &quot;never&quot;],
    // 要求操作符周围有空格
    &quot;space-infix-ops&quot;: 2,
    // 强制在一元操作符前后使用一致的空格
    &quot;space-unary-ops&quot;: [2, {
      &quot;words&quot;: true,
      &quot;nonwords&quot;: false
    }],
    // 强制在注释中 // 或 /* 使用一致的空格
    &quot;spaced-comment&quot;: [2, &quot;always&quot;, {
      &quot;markers&quot;: [&quot;global&quot;, &quot;globals&quot;, &quot;eslint&quot;, &quot;eslint-disable&quot;, &quot;*package&quot;, &quot;!&quot;]
    }],
    // 要求或禁止 Unicode BOM
    &quot;unicode-bom&quot;: 2,
    // 要求正则表达式被括号括起来
    &quot;wrap-regex&quot;: 0,
    //禁止词法声明 (let、const、function 和 class) 出现在 case或default 子句中
    &quot;no-case-declarations&quot;: [&quot;warn&quot;],

    //////////////
    // ES6.相关 //
    //////////////
    // 每个模块只能使用一个import
    &quot;no-duplicate-imports&quot;: 2,
    // 要求箭头函数体使用大括号
    &quot;arrow-body-style&quot;: 2,
    // 要求箭头函数的参数使用圆括号
    &quot;arrow-parens&quot;: 2,
    &quot;arrow-spacing&quot;: [2, {
      &quot;before&quot;: true,
      &quot;after&quot;: true
    }],
    // 强制 generator 函数中 * 号周围使用一致的空格
    &quot;generator-star-spacing&quot;: [2, {
      &quot;before&quot;: true,
      &quot;after&quot;: true
    }],
    // 禁止修改类声明的变量
    &quot;no-class-assign&quot;: 2,
    // 不允许箭头功能，在那里他们可以混淆的比较
    &quot;no-confusing-arrow&quot;: 0,
    // 禁止修改 const 声明的变量
    &quot;no-const-assign&quot;: 2,
    // 禁止类成员中出现重复的名称
    &quot;no-dupe-class-members&quot;: 2,
    // 禁止 Symbolnew 操作符和 new 一起使用
    &quot;no-new-symbol&quot;: 2,
    // 允许指定模块加载时的进口
    &quot;no-restricted-imports&quot;: 0,
    // 禁止在构造函数中，在调用 super() 之前使用 this 或 super
    &quot;no-this-before-super&quot;: 2,
    // 禁止不必要的计算性能键对象的文字
    &quot;no-useless-computed-key&quot;: 0,
    // 要求使用 let 或 const 而不是 var
    &quot;no-var&quot;: 1,
    // 要求或禁止对象字面量中方法和属性使用简写语法
    &quot;object-shorthand&quot;: 0,
    // 要求使用箭头函数作为回调
    &quot;prefer-arrow-callback&quot;: 0,
    // 要求使用 const 声明那些声明后不再被修改的变量
    &quot;prefer-const&quot;: 0,
    // 要求在合适的地方使用 Reflect 方法
    &quot;prefer-reflect&quot;: 0,
    // 要求使用扩展运算符而非 .apply()
    &quot;prefer-spread&quot;: 0,
    // 要求使用模板字面量而非字符串连接
    &quot;prefer-template&quot;: 0,
    // Suggest using the rest parameters instead of arguments
    &quot;prefer-rest-params&quot;: 0,
    // 要求generator 函数内有 yield
    &quot;require-yield&quot;: 2,
    // 要求或禁止模板字符串中的嵌入表达式周围空格的使用
    &quot;template-curly-spacing&quot;: 1,
    // 强制在 yield* 表达式中 * 周围使用空格
    &quot;yield-star-spacing&quot;: 2,
    // 要求或禁止使用拖尾逗号
    &quot;comma-dangle&quot;: 2,
    // 禁止在条件中使用常量表达式
    &quot;no-constant-condition&quot;: 0,
    // 强制一行的最大长度
    &quot;max-len&quot;: [0, 200, {&quot;ignoreUrls&quot;: true}],
    // 禁止出现未使用过的变量
    &quot;no-unused-vars&quot;: [0, {
      &quot;vars&quot;: &quot;all&quot;,// all 检测所有变量，包括全局环境中的变量。这是默认值。
      &quot;args&quot;: &quot;none&quot; // none - 不检查参数。
    }],
  }
};

</code></pre>
<h4 id="6-修改-packagejson">6. 修改 package.json</h4>
<pre><code class="language-javascript"> &quot;scripts&quot;: {
    &quot;lint&quot;: &quot;npx eslint --fix **/*.js&quot;
  },
</code></pre>
<h4 id="7-创建完packagejson文件就可以执行如下命令对指定的文件进行eslint管理">7. 创建完package.json文件，就可以执行如下命令对指定的文件进行eslint管理：</h4>
<pre><code class="language-shell">npm run lint
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[微信小程序使用vant]]></title>
        <id>https://wuc0714.top/post/wei-xin-xiao-cheng-xu-shi-yong-vant/</id>
        <link href="https://wuc0714.top/post/wei-xin-xiao-cheng-xu-shi-yong-vant/">
        </link>
        <updated>2021-04-20T15:35:44.000Z</updated>
        <content type="html"><![CDATA[<h2 id="微信小程序使用vant构建">微信小程序使用Vant构建</h2>
<h3 id="1-创建好后使用-npm-init-命令构建packagejson文件">1. 创建好后使用 npm init 命令构建package.json文件</h3>
<h3 id="2-安装vant">2. 安装vant</h3>
<pre><code># 通过 npm 安装
npm i @vant/weapp -S --production

# 通过 yarn 安装
yarn add @vant/weapp --production

# 安装 0.x 版本
npm i vant-weapp -S --production
</code></pre>
<h3 id="3-修改-appjson">3. 修改 app.json</h3>
<ul>
<li>将 app.json 中的 <code>&quot;style&quot;: &quot;v2&quot;</code> 去除，小程序的<a href="https://developers.weixin.qq.com/miniprogram/dev/reference/configuration/app.html#style">新版基础组件</a>强行加上了许多样式，难以覆盖，不关闭将造成部分组件样式混乱。</li>
</ul>
<h3 id="4-步骤三-修改-projectconfigjson">4. 步骤三 修改 project.config.json</h3>
<p>开发者工具创建的项目，miniprogramRoot 默认为 miniprogram，package.json 在其外部，npm 构建无法正常工作。</p>
<p>需要手动在 project.config.json 内添加如下配置，使开发者工具可以正确索引到 npm 依赖的位置。</p>
<pre><code class="language-json">{
  ...
  &quot;setting&quot;: {
    ...
    &quot;packNpmManually&quot;: true,
    &quot;packNpmRelationList&quot;: [
      {
        &quot;packageJsonPath&quot;: &quot;./package.json&quot;,
        &quot;miniprogramNpmDistDir&quot;: &quot;./&quot;
      }
    ]
  }
}
</code></pre>
<p>注意： 由于目前新版开发者工具创建的小程序目录文件结构问题，npm构建的文件目录为miniprogram_npm，并且开发工具会默认在当前目录下创建miniprogram_npm的文件名，所以新版本的miniprogramNpmDistDir配置为'./'即可</p>
<h3 id="5-构建-npm-包-打开微信开发者工具点击-工具-构建-npm并勾选-使用-npm-模块-选项构建完成后即可引入组件">5. 构建 npm 包  打开微信开发者工具，点击 工具 -&gt; 构建 npm，并勾选 使用 npm 模块 选项，构建完成后，即可引入组件。</h3>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/yz0812/mypic/raw/master/20210408011924.png" alt="" loading="lazy"></figure>
<h3 id="6-使用">6. 使用</h3>
<h3 id="引入组件">引入组件</h3>
<p>以 Button 组件为例，只需要在<code>app.json</code>或<code>index.json</code>中配置 Button 对应的路径即可。</p>
<p>所有组件文档中的引入路径均以 npm 安装为例，如果你是通过下载源代码的方式使用 @vant/weapp，请将路径修改为项目中 @vant/weapp 所在的目录。</p>
<pre><code class="language-json">// 通过 npm 安装
// app.json
&quot;usingComponents&quot;: {
  &quot;van-button&quot;: &quot;@vant/weapp/button/index&quot;
}
// 通过下载源码使用 es6版本
// app.json
&quot;usingComponents&quot;: {
  &quot;van-button&quot;: &quot;path/to/@vant/weapp/dist/button/index&quot;
}
// 通过下载源码使用 es5版本
// app.json
&quot;usingComponents&quot;: {
  &quot;van-button&quot;: &quot;path/to/@vant/weapp/lib/button/index&quot;
}
</code></pre>
<h3 id="使用组件">使用组件</h3>
<p>引入组件后，可以在 wxml 中直接使用组件</p>
<pre><code class="language-xml">&lt;van-button type=&quot;primary&quot;&gt;按钮&lt;/van-button&gt;
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ docker 安装nginx]]></title>
        <id>https://wuc0714.top/post/docker-an-zhuang-nginx/</id>
        <link href="https://wuc0714.top/post/docker-an-zhuang-nginx/">
        </link>
        <updated>2021-04-07T14:06:13.000Z</updated>
        <content type="html"><![CDATA[<h2 id="docker-安装nginx">docker 安装nginx</h2>
<p>先上docker-compose 配置</p>
<pre><code class="language-yaml">  nginx:
    #restart: always
    image: nginx
    container_name: nginx
    ports:
      - 80:80
      - 443:443
    links:
      - tomcat:tomcat
    volumes:
      - /Users/docker/yz/nginx/nginx.conf:/etc/nginx/nginx.conf #配置文件
      - /Users/docker/yz/nginx/conf.d:/etc/nginx/conf.d #配置文件
      - /Users/docker/yz/nginx/log:/var/log/nginx #日志地址
      - /Users/docker/yz/nginxWeb:/Users/docker/yz/nginxWeb #映射虚拟地址

  tomcat: 
    image: tomcat:9.0
    container_name: tomcat9.0
    #restart: always
    ports:
      - 8080:8080
    volumes:
      - /Users/docker/yz/tomcat9.0/logs:/usr/local/tomcat/logs
      - /Users/docker/yz/tomcat9.0/webapps:/usr/local/tomcat/webapps
</code></pre>
<ul>
<li>
<p>nginx正向代理一直显示文件404,找不到文件地址,原因是没有在docker-compose.yml里面做映射</p>
</li>
<li>
<p>nginx反向代理需要在docker-compose.yml配置links 然后再在 nginx.conf 配置</p>
<pre><code class="language-yaml">    upstream tomcat_client {
        server tomcat:8080 weight=1;
    } 
    server {
        listen       80;

        location / {
            proxy_pass   http://tomcat_client;
            index  index.html index.htm;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }
    }
</code></pre>
</li>
<li>
<p>nginx docker使用反向代理</p>
</li>
</ul>
<p>server_name 在mac中无法使用localhost,127.0.0.1</p>
<pre><code class="language-yaml">  server {
        listen       80;
        server_name  192.168.3.33;
        location / {
            proxy_set_header Host $host;
            proxy_pass http://192.168.3.33:8761;
        }
    }
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[vue bug]]></title>
        <id>https://wuc0714.top/post/vue-bug/</id>
        <link href="https://wuc0714.top/post/vue-bug/">
        </link>
        <updated>2021-04-07T14:05:52.000Z</updated>
        <content type="html"><![CDATA[<p>element bug</p>
<p>input text无法编辑,v-model绑定的值没有定义</p>
<p>input text读取之后无法编辑</p>
<p>原因在Vue实例创建时，v-model绑定的值并未声明，因此就没有被Vue转换为响应式的属性，自然就不会触发视图的更新</p>
<pre><code class="language-javascript">      this.$set(this.remarkInfoForm, 'remarkContent', row.remarkContent)
</code></pre>
]]></content>
    </entry>
</feed>