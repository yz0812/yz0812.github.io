<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://yz0812.github.io</id>
    <title>yz</title>
    <updated>2024-01-20T17:18:01.730Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://yz0812.github.io"/>
    <link rel="self" href="https://yz0812.github.io/atom.xml"/>
    <subtitle>少年拥有了时间,时间带走了年少</subtitle>
    <logo>https://yz0812.github.io/images/avatar.png</logo>
    <icon>https://yz0812.github.io/favicon.ico</icon>
    <rights>All rights reserved 2024, yz</rights>
    <entry>
        <title type="html"><![CDATA[ oh-my-zsh 安装]]></title>
        <id>https://yz0812.github.io/post/oh-my-zsh-an-zhuang/</id>
        <link href="https://yz0812.github.io/post/oh-my-zsh-an-zhuang/">
        </link>
        <updated>2023-09-19T20:52:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="oh-my-zsh-安装">oh-my-zsh 安装</h2>
<h3 id="1安装-zsh">1.安装 zsh</h3>
<p><code>linux</code> 下：</p>
<pre><code class="language-shell"> yum install zsh
</code></pre>
<p><code>mac</code>下：</p>
<pre><code class="language-shell">brew install zsh
</code></pre>
<h3 id="2安装-oh-my-zsh">2.安装 oh-my-zsh</h3>
<pre><code class="language-shell"># 这里需要下载的是https://github.com/ohmyzsh/ohmyzsh ,但是github因为某些原因下不来,所以后面都使用gitee
wget https://gitee.com/mirrors/oh-my-zsh/raw/master/tools/install.sh
# 然后给install.sh添加权限：
chmod +x install.sh
</code></pre>
<p>编辑 install.sh</p>
<pre><code class="language-shell">vim install.sh
</code></pre>
<p>找到以下部分修改：</p>
<pre><code class="language-sh"># Default settings
REPO=${REPO:-ohmyzsh/ohmyzsh}
REMOTE=${REMOTE:-https://github.com/${REPO}.git}
BRANCH=${BRANCH:-master}
# 修改其中的github为gitee,还有-ohmyzsh/ohmyzsh修改为-mirrors/oh-my-zsh
REPO=${REPO:-mirrors/oh-my-zsh}
REMOTE=${REMOTE:-https://gitee.com/${REPO}.git}
</code></pre>
<p>然后执行<code> ./install.sh</code></p>
<h3 id="3配置-zsh">3.配置 zsh</h3>
<pre><code class="language-shell">vim ~/.zshrc
</code></pre>
<p>修改主题：</p>
<pre><code class="language-sh">ZSH_THEME=&quot;robbyrussell&quot;
# 改为
ZSH_THEME=&quot;ys&quot;
</code></pre>
<p>修改插件：</p>
<pre><code class="language-sh">plugins=(git)
# 改为
plugins=(git zsh-syntax-highlighting zsh-autosuggestions)
</code></pre>
<h3 id="4-下载插件">4. 下载插件</h3>
<pre><code class="language-shell">git clone https://gitee.com/fast-mirrors/zsh-syntax-highlighting.git ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting


git clone https://gitee.com/fast-mirrors/zsh-autosuggestions ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions
</code></pre>
<h3 id="5刷新">5.刷新</h3>
<pre><code class="language-shell">source ~/.bash_profile
source ~/.zshrc
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ 经纬度计算距离]]></title>
        <id>https://yz0812.github.io/post/jing-wei-du-ji-suan-ju-chi/</id>
        <link href="https://yz0812.github.io/post/jing-wei-du-ji-suan-ju-chi/">
        </link>
        <updated>2023-09-14T16:11:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="经纬度计算距离">经纬度计算距离</h2>
<h3 id="1导入maven依赖">1.导入maven依赖</h3>
<p><code>org.gavaghan</code> 是一个 Java 软件包，由 Tim Gavaghan 创建和维护。该软件包提供了一系列用于处理和解析各种数据格式的类和工具。</p>
<pre><code class="language-xml">     &lt;dependency&gt;
          &lt;groupId&gt;org.gavaghan&lt;/groupId&gt;
          &lt;artifactId&gt;geodesy&lt;/artifactId&gt;
          &lt;version&gt;${geodesy.version}&lt;/version&gt;
     &lt;/dependency&gt;
</code></pre>
<h3 id="2代码实现">2.代码实现</h3>
<pre><code class="language-java">
  GlobalCoordinates start = globalCoordinatesList.get(i);
            GlobalCoordinates end = globalCoordinatesList.get(i + 1);


    private static BigDecimal getDistanceMeter(GlobalCoordinates gpsFrom, GlobalCoordinates gpsTo) {
        //创建GeodeticCalculator，调用计算方法，传入坐标系、经纬度用于计算距离
        GeodeticCurve geoCurve = new GeodeticCalculator().calculateGeodeticCurve(Ellipsoid.Sphere, gpsFrom, gpsTo);
        return BigDecimal.valueOf(geoCurve.getEllipsoidalDistance());
    }

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[flowable 流程跟踪]]></title>
        <id>https://yz0812.github.io/post/flowable-liu-cheng-gen-zong/</id>
        <link href="https://yz0812.github.io/post/flowable-liu-cheng-gen-zong/">
        </link>
        <updated>2023-09-14T15:58:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="flowable-流程跟踪">flowable 流程跟踪</h2>
<pre><code class="language-java">import cn.business.framework.common.constant.StringConstant;
import cn.business.framework.common.exception.ServiceException;
import cn.business.framework.flowable.core.util.FlowableUtils;
import cn.business.module.bpm.controller.admin.definition.vo.model.BpmModelRespVO;
import cn.business.module.bpm.service.definition.BpmModelService;
import cn.business.module.bpm.util.ELUtil;
import cn.hutool.core.util.StrUtil;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.apache.commons.lang3.StringUtils;
import org.flowable.bpmn.converter.BpmnXMLConverter;
import org.flowable.bpmn.model.*;
import org.flowable.engine.HistoryService;
import org.flowable.engine.RepositoryService;
import org.flowable.engine.RuntimeService;
import org.flowable.engine.history.HistoricProcessInstance;
import org.flowable.engine.runtime.ProcessInstance;
import org.springframework.stereotype.Service;

import java.util.*;
import java.util.stream.Collectors;

import static cn.business.module.bpm.enums.ErrorCodeConstants.PROCESS_NODE_CONFIGURATION_ERROR;

/**
 * @author yuanzhang
 * @description 流程服务
 * @date 2023/08/22
 **/
@Service
@Slf4j
@RequiredArgsConstructor
public class FlowAbleService {
    private final RuntimeService runtimeService;
    private final RepositoryService repositoryService;
    private final BpmModelService bpmModelService;
    private final HistoryService historyService;

    /**
     * @param processInstanceId 流程实例id
     * @param modelId           模型id
     * @param variableMap       变量映射
     * @return {@link List }&lt;{@link FlowElement }&gt;
     * @description 通过变量推算流程走向
     * @author yuanzhang
     * @date 2023/08/02
     **/
    public List&lt;FlowElement&gt; calApprovePath(String processInstanceId, String modelId, Map&lt;String, Object&gt; variableMap) {
        BpmnModel bpmnModel;
        if (StringUtils.isNotBlank(processInstanceId)) {
            ProcessInstance processInstance = runtimeService.createProcessInstanceQuery().processInstanceId(processInstanceId).singleResult();
            if (null == processInstance) {
                HistoricProcessInstance historicProcessInstance = historyService.createHistoricProcessInstanceQuery().processInstanceId(processInstanceId).singleResult();
                bpmnModel = repositoryService.getBpmnModel(historicProcessInstance.getProcessDefinitionId());
            } else {
                bpmnModel = repositoryService.getBpmnModel(processInstance.getProcessDefinitionId());
            }
            BpmnXMLConverter xmlConverter = new BpmnXMLConverter();
            byte[] xmlBytes = xmlConverter.convertToXML(bpmnModel);
            String xmlData = new String(xmlBytes);
            bpmnModel = FlowableUtils.parseBpmnXml(xmlData);
        } else {
            BpmModelRespVO model = bpmModelService.getModel(modelId);
            bpmnModel = bpmModelService.getBpmnModel(model.getId());
        }
        Collection&lt;FlowElement&gt; flowElements = bpmnModel.getMainProcess().getFlowElements();
        List&lt;FlowElement&gt; passElements = new ArrayList&lt;&gt;();
        this.dueStartElement(passElements, flowElements, variableMap);
        return passElements;
    }

    /**
     * 2. 找到开始节点，通过它的目标节点，然后再不断往下找。
     */
    private void dueStartElement(List&lt;FlowElement&gt; passElements, Collection&lt;FlowElement&gt; flowElements, Map&lt;String, Object&gt; variableMap) {
        flowElements.stream().filter(StartEvent.class::isInstance).findFirst().ifPresent(startElement -&gt; {
            flowElements.remove(startElement);
            List&lt;SequenceFlow&gt; outgoingFlows = ((StartEvent) startElement).getOutgoingFlows();
            String targetRef = outgoingFlows.get(0).getTargetRef();
            // 根据ID找到FlowElement
            FlowElement targetElementOfStartElement = getFlowElement(flowElements, targetRef);
            if (targetElementOfStartElement instanceof UserTask || targetElementOfStartElement instanceof ExclusiveGateway) {
                this.getPassElementList(passElements, flowElements, targetElementOfStartElement, variableMap);
            }
        });
    }

    /**
     * 3. 我只用到了UserTask、ExclusiveGateway、ParallelGateway，所以代码里只列举了这三种，如果用到了其他的，可以再自己补充
     */
    private void getPassElementList(List&lt;FlowElement&gt; passElements, Collection&lt;FlowElement&gt; flowElements, FlowElement curFlowElement, Map&lt;String, Object&gt; variableMap) {
        // 任务节点
        if (curFlowElement instanceof UserTask) {
            this.dueUserTaskElement(passElements, flowElements, curFlowElement, variableMap);
            return;
        }
        // 排他网关
        if (curFlowElement instanceof ExclusiveGateway) {
            this.dueExclusiveGateway(passElements, flowElements, curFlowElement, variableMap);
            return;
        }
        // 并行网关
        if (curFlowElement instanceof ParallelGateway) {
            this.dueParallelGateway(passElements, flowElements, curFlowElement, variableMap);
        }
    }


    private void dueUserTaskElement(List&lt;FlowElement&gt; passElements, Collection&lt;FlowElement&gt; flowElements, FlowElement curFlowElement, Map&lt;String, Object&gt; variableMap) {
        passElements.add(curFlowElement);
        List&lt;SequenceFlow&gt; outgoingFlows = ((UserTask) curFlowElement).getOutgoingFlows();
        String targetRef = outgoingFlows.get(0).getTargetRef();
        if (outgoingFlows.size() &gt; 1) {
            // 找到表达式成立的sequenceFlow
            SequenceFlow sequenceFlow = getSequenceFlow(passElements, variableMap, outgoingFlows);
            targetRef = sequenceFlow.getTargetRef();
        }
        // 根据ID找到FlowElement
        FlowElement targetElement = getFlowElement(flowElements, targetRef);
        this.getPassElementList(passElements, flowElements, targetElement, variableMap);
    }

    private void dueExclusiveGateway(List&lt;FlowElement&gt; passElements, Collection&lt;FlowElement&gt; flowElements, FlowElement curFlowElement, Map&lt;String, Object&gt; variableMap) {
        // 获取符合条件的sequenceFlow的目标FlowElement
        List&lt;SequenceFlow&gt; exclusiveGatewayOutgoingFlows = ((ExclusiveGateway) curFlowElement).getOutgoingFlows();
        flowElements.remove(curFlowElement);
        // 找到表达式成立的sequenceFlow
        SequenceFlow sequenceFlow = getSequenceFlow(passElements, variableMap, exclusiveGatewayOutgoingFlows);
        // 根据ID找到FlowElement
        FlowElement targetElement = getFlowElement(flowElements, sequenceFlow.getTargetRef());
        this.getPassElementList(passElements, flowElements, targetElement, variableMap);
    }

    private void dueParallelGateway(List&lt;FlowElement&gt; passElements, Collection&lt;FlowElement&gt; flowElements, FlowElement curFlowElement, Map&lt;String, Object&gt; variableMap) {
        FlowElement targetElement;
        List&lt;SequenceFlow&gt; parallelGatewayOutgoingFlows = ((ParallelGateway) curFlowElement).getOutgoingFlows();
        for (SequenceFlow sequenceFlow : parallelGatewayOutgoingFlows) {
            targetElement = getFlowElement(flowElements, sequenceFlow.getTargetRef());
            this.getPassElementList(passElements, flowElements, targetElement, variableMap);
        }
    }

    private FlowElement getFlowElement(Collection&lt;FlowElement&gt; flowElements, String targetRef) {
        return flowElements.stream().filter(flowElement -&gt; targetRef.equals(flowElement.getId())).findFirst().orElse(null);
    }

    /**
     * @param passElements  流程全部节点
     * @param variableMap   参数
     * @param outgoingFlows 流转线路
     * @return {@link SequenceFlow }
     * @description 4. 根据传入的变量，计算出表达式成立的那一条SequenceFlow
     * @author yuanzhang
     * @date 2023/08/14
     **/
    private SequenceFlow getSequenceFlow(List&lt;FlowElement&gt; passElements, Map&lt;String, Object&gt; variableMap, List&lt;SequenceFlow&gt; outgoingFlows) {
        if (outgoingFlows.size() &gt; 1 &amp;&amp; (outgoingFlows.stream().noneMatch(item -&gt; StrUtil.isNotBlank(item.getConditionExpression())))) {
            String string = passElements.stream().filter(element -&gt; StrUtil.equals(element.getId(), outgoingFlows.get(0).getSourceRef())).findFirst().map(FlowElement::getName).orElse(StringConstant.EMPTY);
            throw new ServiceException(PROCESS_NODE_CONFIGURATION_ERROR, string);
        }
        // 优先计算存在表达式的
        List&lt;SequenceFlow&gt; sortedList = outgoingFlows.stream().sorted(
                Comparator.comparing(SequenceFlow::getConditionExpression, Comparator.nullsLast(String::compareTo))
        ).collect(Collectors.toList());
        return sortedList.stream().filter(item -&gt; ELUtil.getValue(variableMap, item.getConditionExpression())).findFirst().orElse(outgoingFlows.get(0));
    }


}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[BaseRedis 工具类]]></title>
        <id>https://yz0812.github.io/post/baseredis-gong-ju-lei/</id>
        <link href="https://yz0812.github.io/post/baseredis-gong-ju-lei/">
        </link>
        <updated>2023-06-11T14:52:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="redisautoconfiguration配置类">RedisAutoConfiguration配置类</h2>
<pre><code class="language-java">
@AutoConfiguration
public class RedisAutoConfiguration {

    /**
     * 创建 RedisTemplate Bean，使用 JSON 序列化方式
     */
    @Bean
    public RedisTemplate&lt;String, Object&gt; redisTemplate(RedisConnectionFactory factory) {
        // 创建 RedisTemplate 对象
        RedisTemplate&lt;String, Object&gt; template = new RedisTemplate&lt;&gt;();
        // 设置 RedisConnection 工厂。😈 它就是实现多种 Java Redis 客户端接入的秘密工厂。感兴趣的胖友，可以自己去撸下。
        template.setConnectionFactory(factory);
        // 使用 String 序列化方式，序列化 KEY 。
        template.setKeySerializer(RedisSerializer.string());
        template.setHashKeySerializer(RedisSerializer.string());
        // 使用 JSON 序列化方式（库是 Jackson ），序列化 VALUE 。
        template.setValueSerializer(RedisSerializer.json());
        template.setHashValueSerializer(RedisSerializer.json());
        return template;
    }

    @Bean
    public BaseRedis baseRedis(RedisTemplate&lt;String, Object&gt; redisTemplate) {
        return new BaseRedis(redisTemplate);
    }
}
</code></pre>
<h2 id="注入配置类">注入配置类</h2>
<p>在<span style='color:red'>resources</span>中创建<span style='color:red'>META-INF.spring</span>目录,创建<span style='color:red'>org.springframework.boot.autoconfigure.AutoConfiguration.imports</span>文件</p>
<h2 id="baseredisjava">BaseRedis.java</h2>
<pre><code class="language-java">
import org.springframework.data.redis.core.*;
import org.springframework.data.redis.core.ZSetOperations.TypedTuple;
import org.springframework.lang.Nullable;
import org.springframework.util.Assert;

import java.time.Duration;
import java.util.*;
import java.util.concurrent.TimeUnit;
import java.util.function.Supplier;

public class BaseRedis {
    private final RedisTemplate&lt;String, Object&gt; redisTemplate;
    private final ValueOperations&lt;String, Object&gt; valueOps;
    private final HashOperations&lt;String, Object, Object&gt; hashOps;
    private final ListOperations&lt;String, Object&gt; listOps;
    private final SetOperations&lt;String, Object&gt; setOps;
    private final ZSetOperations&lt;String, Object&gt; zSetOps;

    public BaseRedis(RedisTemplate&lt;String, Object&gt; redisTemplate) {
        this.redisTemplate = redisTemplate;
        Assert.notNull(redisTemplate, &quot;redisTemplate is null&quot;);
        this.valueOps = redisTemplate.opsForValue();
        this.hashOps = redisTemplate.opsForHash();
        this.listOps = redisTemplate.opsForList();
        this.setOps = redisTemplate.opsForSet();
        this.zSetOps = redisTemplate.opsForZSet();
    }

    public void set(CacheKey cacheKey, Object value) {
        String key = cacheKey.getKey();
        Duration expire = cacheKey.getExpire();
        if (expire == null) {
            this.set(key, value);
        } else {
            this.setEx(key, value, expire);
        }

    }

    public void set(String key, Object value) {
        this.valueOps.set(key, value);
    }

    public void setEx(String key, Object value, Duration timeout) {
        this.valueOps.set(key, value, timeout);
    }

    public void setEx(String key, Object value, Long seconds) {
        this.valueOps.set(key, value, seconds, TimeUnit.SECONDS);
    }

    @Nullable
    public &lt;T&gt; T get(String key) {
        return (T) this.valueOps.get(key);
    }

    @Nullable
    public &lt;T&gt; T get(String key, Supplier&lt;T&gt; loader) {
        T value = this.get(key);
        if (value != null) {
            return value;
        } else {
            value = loader.get();
            if (value == null) {
                return null;
            } else {
                this.set(key, value);
                return value;
            }
        }
    }

    @Nullable
    public &lt;T&gt; T get(CacheKey cacheKey) {
        return (T) this.valueOps.get(cacheKey.getKey());
    }

    @Nullable
    public &lt;T&gt; T get(CacheKey cacheKey, Supplier&lt;T&gt; loader) {
        String key = cacheKey.getKey();
        T value = this.get(key);
        if (value != null) {
            return value;
        } else {
            value = loader.get();
            if (value == null) {
                return null;
            } else {
                this.set(cacheKey, value);
                return value;
            }
        }
    }

    public Boolean del(String key) {
        return this.redisTemplate.delete(key);
    }

    public Boolean del(CacheKey key) {
        return this.redisTemplate.delete(key.getKey());
    }

    public Long del(String... keys) {
        return this.del(Arrays.asList(keys));
    }

    public Long del(Collection&lt;String&gt; keys) {
        return this.redisTemplate.delete(keys);
    }

    public Set&lt;String&gt; keys(String pattern) {
        return this.redisTemplate.keys(pattern);
    }

    public void mSet(Object... keysValues) {
        this.valueOps.multiSet(toMap(keysValues));
    }

    public List&lt;Object&gt; mGet(String... keys) {
        return this.mGet(Arrays.asList(keys));
    }

    public List&lt;Object&gt; mGet(Collection&lt;String&gt; keys) {
        return this.valueOps.multiGet(keys);
    }

    public Long decr(String key) {
        return this.valueOps.decrement(key);
    }

    public Long decrBy(String key, long longValue) {
        return this.valueOps.decrement(key, longValue);
    }

    public Long incr(String key) {
        return this.valueOps.increment(key);
    }

    public Long incrBy(String key, long longValue) {
        return this.valueOps.increment(key, longValue);
    }

    public Long getCounter(String key) {
        return Long.valueOf(String.valueOf(this.valueOps.get(key)));
    }

    public Boolean exists(String key) {
        return this.redisTemplate.hasKey(key);
    }

    public String randomKey() {
        return (String) this.redisTemplate.randomKey();
    }

    public void rename(String oldkey, String newkey) {
        this.redisTemplate.rename(oldkey, newkey);
    }

    public Boolean move(String key, int dbIndex) {
        return this.redisTemplate.move(key, dbIndex);
    }

    public Boolean expire(String key, long seconds) {
        return this.redisTemplate.expire(key, seconds, TimeUnit.SECONDS);
    }

    public Boolean expire(String key, Duration timeout) {
        return this.expire(key, timeout.getSeconds());
    }

    public Boolean expireAt(String key, Date date) {
        return this.redisTemplate.expireAt(key, date);
    }

    public Boolean expireAt(String key, long unixTime) {
        return this.expireAt(key, new Date(unixTime));
    }

    public Boolean pexpire(String key, long milliseconds) {
        return this.redisTemplate.expire(key, milliseconds, TimeUnit.MILLISECONDS);
    }

    public &lt;T&gt; T getSet(String key, Object value) {
        return (T) this.valueOps.getAndSet(key, value);
    }

    public Boolean persist(String key) {
        return this.redisTemplate.persist(key);
    }

    public String type(String key) {
        return this.redisTemplate.type(key).code();
    }

    public Long ttl(String key) {
        return this.redisTemplate.getExpire(key);
    }

    public Long pttl(String key) {
        return this.redisTemplate.getExpire(key, TimeUnit.MILLISECONDS);
    }

    public void hSet(String key, Object field, Object value) {
        this.hashOps.put(key, field, value);
    }

//    public void hMset(String key, Map&lt;Object, Object&gt; hash) {
//        this.hashOps.putAll(key, hash);
//    }

    public &lt;K, V&gt; void hMset(String key, Map&lt;K, V&gt; hash) {
        this.hashOps.putAll(key, hash);
    }

    public &lt;T&gt; T hGet(String key, Object field) {
        return (T) this.hashOps.get(key, field);
    }

    public List hmGet(String key, Object... fields) {
        return this.hmGet(key, (Collection) Arrays.asList(fields));
    }

    public List hmGet(String key, Collection&lt;Object&gt; hashKeys) {
        return this.hashOps.multiGet(key, hashKeys);
    }

    public Long hDel(String key, Object... fields) {
        return this.hashOps.delete(key, fields);
    }

    public Boolean hExists(String key, Object field) {
        return this.hashOps.hasKey(key, field);
    }

    public Map hGetAll(String key) {
        return this.hashOps.entries(key);
    }

    public List hVals(String key) {
        return this.hashOps.values(key);
    }

    public Set&lt;Object&gt; hKeys(String key) {
        return this.hashOps.keys(key);
    }

    public Long hLen(String key) {
        return this.hashOps.size(key);
    }

    public Long hIncrBy(String key, Object field, long value) {
        return this.hashOps.increment(key, field, value);
    }

    public Double hIncrByFloat(String key, Object field, double value) {
        return this.hashOps.increment(key, field, value);
    }

    public &lt;T&gt; T lIndex(String key, long index) {
        return (T) this.listOps.index(key, index);
    }

    public Long lLen(String key) {
        return this.listOps.size(key);
    }

    public &lt;T&gt; T lPop(String key) {
        return (T) this.listOps.leftPop(key);
    }

    public Long lPush(String key, Object... values) {
        return this.listOps.leftPush(key, values);
    }

    public &lt;T&gt; Long lPush(String key, T values) {
        return this.listOps.leftPush(key, values);
    }

    public &lt;T&gt; Long lPushAll(String key, T... values) {
        return this.listOps.leftPushAll(key, values);
    }

    public void lSet(String key, long index, Object value) {
        this.listOps.set(key, index, value);
    }

    public Long lRem(String key, long count, Object value) {
        return this.listOps.remove(key, count, value);
    }

    public List lRange(String key, long start, long end) {
        return this.listOps.range(key, start, end);
    }

    public void lTrim(String key, long start, long end) {
        this.listOps.trim(key, start, end);
    }

    public &lt;T&gt; T rPop(String key) {
        return (T) this.listOps.rightPop(key);
    }

    public Long rPush(String key, Object... values) {
        return this.listOps.rightPush(key, values);
    }

    public &lt;T&gt; T rPopLPush(String srcKey, String dstKey) {
        return (T) this.listOps.rightPopAndLeftPush(srcKey, dstKey);
    }

    public Long sAdd(String key, Object... members) {
        return this.setOps.add(key, members);
    }

    public &lt;T&gt; T sPop(String key) {
        return (T) this.setOps.pop(key);
    }

    public Set sMembers(String key) {
        return this.setOps.members(key);
    }

    public Long sSize(String key) {
        return this.setOps.size(key);
    }

    public boolean sIsMember(String key, Object member) {
        return this.setOps.isMember(key, member);
    }

    public Set sInter(String key, String otherKey) {
        return this.setOps.intersect(key, otherKey);
    }

    public Set sInter(String key, Collection&lt;String&gt; otherKeys) {
        return this.setOps.intersect(key, otherKeys);
    }

    public &lt;T&gt; T sRandMember(String key) {
        return (T) this.setOps.randomMember(key);
    }

    public List sRandMember(String key, int count) {
        return this.setOps.randomMembers(key, (long) count);
    }

    public Long sRem(String key, Object... members) {
        return this.setOps.remove(key, members);
    }

    public Set sUnion(String key, String otherKey) {
        return this.setOps.union(key, otherKey);
    }

    public Set sUnion(String key, Collection&lt;String&gt; otherKeys) {
        return this.setOps.union(key, otherKeys);
    }

    public Set sDiff(String key, String otherKey) {
        return this.setOps.difference(key, otherKey);
    }

    public Set sDiff(String key, Collection&lt;String&gt; otherKeys) {
        return this.setOps.difference(key, otherKeys);
    }

    public Boolean zAdd(String key, Object member, double score) {
        return this.zSetOps.add(key, member, score);
    }

    public Long zAdd(String key, Map&lt;Object, Double&gt; scoreMembers) {
        Set&lt;TypedTuple&lt;Object&gt;&gt; tuples = new HashSet&lt;&gt;();
        scoreMembers.forEach((k, v) -&gt; {
            tuples.add(new DefaultTypedTuple(k, v));
        });
        return this.zSetOps.add(key, tuples);
    }

    public Long zCard(String key) {
        return this.zSetOps.zCard(key);
    }

    public Long zCount(String key, double min, double max) {
        return this.zSetOps.count(key, min, max);
    }

    public Double zIncrBy(String key, Object member, double score) {
        return this.zSetOps.incrementScore(key, member, score);
    }

    public Set zRange(String key, long start, long end) {
        return this.zSetOps.range(key, start, end);
    }

    public Set zRevrange(String key, long start, long end) {
        return this.zSetOps.reverseRange(key, start, end);
    }

    public Set zRangeByScore(String key, double min, double max) {
        return this.zSetOps.rangeByScore(key, min, max);
    }

    public Long zRank(String key, Object member) {
        return this.zSetOps.rank(key, member);
    }

    public Long zRevrank(String key, Object member) {
        return this.zSetOps.reverseRank(key, member);
    }

    public Long zRem(String key, Object... members) {
        return this.zSetOps.remove(key, members);
    }

    public Double zScore(String key, Object member) {
        return this.zSetOps.score(key, member);
    }

    public RedisTemplate&lt;String, Object&gt; getRedisTemplate() {
        return this.redisTemplate;
    }

    public ValueOperations&lt;String, Object&gt; getValueOps() {
        return this.valueOps;
    }

    public HashOperations&lt;String, Object, Object&gt; getHashOps() {
        return this.hashOps;
    }

    public ListOperations&lt;String, Object&gt; getListOps() {
        return this.listOps;
    }

    public SetOperations&lt;String, Object&gt; getSetOps() {
        return this.setOps;
    }

    public ZSetOperations&lt;String, Object&gt; getZSetOps() {
        return this.zSetOps;
    }


    private static &lt;K, V&gt; Map&lt;K, V&gt; toMap(Object... keysValues) {
        int kvLength = keysValues.length;
        if (kvLength % 2 != 0) {
            throw new IllegalArgumentException(&quot;wrong number of arguments for met, keysValues length can not be odd&quot;);
        } else {
            Map&lt;K, V&gt; keyValueMap = new HashMap(kvLength);

            for (int i = kvLength - 2; i &gt;= 0; i -= 2) {
                Object key = keysValues[i];
                Object value = keysValues[i + 1];
                keyValueMap.put((K) key, (V) value);
            }

            return keyValueMap;
        }
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[docker-compose安装gitlab-ee]]></title>
        <id>https://yz0812.github.io/post/docker-compose-an-zhuang-gitlab-ee/</id>
        <link href="https://yz0812.github.io/post/docker-compose-an-zhuang-gitlab-ee/">
        </link>
        <updated>2023-06-11T14:51:01.000Z</updated>
        <content type="html"><![CDATA[<h1 id="docker-compose安装gitlab-ee以及优化方案">docker-compose安装gitlab-ee以及优化方案</h1>
<h2 id="简略版">简略版</h2>
<pre><code class="language-yaml">version: '3.6'
services:
  web:
    image: 'gitlab/gitlab-ee:latest'   #下载慢可替换加速地址
    restart: always
    hostname: 'demo.com'   #此处建议填写访问域名
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'http://127.0.0.1:8088'   #此处是gitlab访问地址填写域名，IP也可以
        gitlab_rails['time_zone'] = 'Asia/Shanghai'
        # Add any other gitlab.rb configuration here, each on its own line
        
    ports:
      - '8088:80'    #容器内80端口转发到主机8088端口
      - '8443:443'   #容器内443端口转发到主机8443端口
      - '222:22'   #容器内22端口转发到主机222端口
    volumes:
      - '$GITLAB_HOME/gitlab/config:/etc/gitlab'
      - '$GITLAB_HOME/gitlab/logs:/var/log/gitlab'
      - '$GITLAB_HOME/gitlab/data:/var/opt/gitlab'
 
 
    shm_size: '1g'   #官网默认64m，容器日志会出错建议增加
</code></pre>
<h2 id="优化版">优化版</h2>
<pre><code class="language-yaml">version: '3.6'
services:
  web:
    image: 'gitlab/gitlab-ee:latest'
    restart: always
    hostname: 'demo.com'
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'http://127.0.0.1:8088'
        gitlab_rails['time_zone'] = 'Asia/Shanghai'
        # Add any other gitlab.rb configuration here, each on its own line
        
        # 关闭电子邮件相关功能
        gitlab_rails['smtp_enable'] = false
        gitlab_rails['gitlab_email_enabled'] = false
        gitlab_rails['incoming_email_enabled'] = false
 
        # Terraform
        gitlab_rails['terraform_state_enabled'] = false
 
        # 使用情况统计
        gitlab_rails['usage_ping_enabled'] = false
        gitlab_rails['sentry_enabled'] = false
        grafana['reporting_enabled'] = false
 
        # 关闭容器仓库功能
        gitlab_rails['gitlab_default_projects_features_container_registry'] = false
        gitlab_rails['registry_enabled'] = false
        registry['enable'] = false
        registry_nginx['enable'] = false
 
        # 包仓库
        gitlab_rails['packages_enabled'] = false
        gitlab_rails['dependency_proxy_enabled'] = false
 
        # GitLab KAS
        gitlab_kas['enable'] = false
        gitlab_rails['gitlab_kas_enabled'] = false
 
        # Mattermost
        mattermost['enable'] = false
        mattermost_nginx['enable'] = false
 
        # Kerberos
        gitlab_rails['kerberos_enabled'] = false
        sentinel['enable'] = false
 
        # GitLab Pages
        gitlab_pages['enable'] = false
        pages_nginx['enable'] = false
 
        # 禁用 PUMA 集群模式
        puma['worker_processes'] = 0
        puma['min_threads'] = 1
        puma['max_threads'] = 2
 
        # 降低后台守护进程并发数
        sidekiq['max_concurrency'] = 5
 
        gitlab_ci['gitlab_ci_all_broken_builds'] = false
        gitlab_ci['gitlab_ci_add_pusher'] = false
 
        # 关闭监控
        prometheus_monitoring['enable'] = false
        alertmanager['enable'] = false
        node_exporter['enable'] = false
        redis_exporter['enable'] = false
        postgres_exporter['enable'] = false
        pgbouncer_exporter['enable'] = false
        gitlab_exporter['enable'] = false
        grafana['enable'] = false
        sidekiq['metrics_enabled'] = false
 
 
        # 配置 GitLab 处理内存的方式
        gitlab_rails['env'] = {
          'MALLOC_CONF' =&gt; 'dirty_decay_ms:1000,muzzy_decay_ms:1000'
        }
 
        gitaly['env'] = {
          'MALLOC_CONF' =&gt; 'dirty_decay_ms:1000,muzzy_decay_ms:1000'
        }
    ports:
      - '8088:80'
      - '8443:443'
      - '222:22'
    volumes:
      - '$GITLAB_HOME/gitlab/config:/etc/gitlab'
      - '$GITLAB_HOME/gitlab/logs:/var/log/gitlab'
      - '$GITLAB_HOME/gitlab/data:/var/opt/gitlab'
 
 
    shm_size: '1g'
</code></pre>
<h2 id="gitlab-ee高级功能">gitlab-ee高级功能</h2>
<pre><code class="language-yaml">version: '3.6'
services:
  web:
    image: 'gitlab/gitlab-ee:latest'
    restart: always
    hostname: 'demo.com'
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'http://127.0.0.1:8088'
        gitlab_rails['time_zone'] = 'Asia/Shanghai'
        # Add any other gitlab.rb configuration here, each on its own line
        
        # 关闭电子邮件相关功能
        gitlab_rails['smtp_enable'] = false
        gitlab_rails['gitlab_email_enabled'] = false
        gitlab_rails['incoming_email_enabled'] = false
 
        # Terraform
        gitlab_rails['terraform_state_enabled'] = false
 
        # Usage Statistics
        gitlab_rails['usage_ping_enabled'] = false
        gitlab_rails['sentry_enabled'] = false
        grafana['reporting_enabled'] = false
 
        # 关闭容器仓库功能
        gitlab_rails['gitlab_default_projects_features_container_registry'] = false
        gitlab_rails['registry_enabled'] = false
        registry['enable'] = false
        registry_nginx['enable'] = false
 
        # 包仓库
        gitlab_rails['packages_enabled'] = false
        gitlab_rails['dependency_proxy_enabled'] = false
 
        # GitLab KAS
        gitlab_kas['enable'] = false
        gitlab_rails['gitlab_kas_enabled'] = false
 
        # Mattermost
        mattermost['enable'] = false
        mattermost_nginx['enable'] = false
 
        # Kerberos
        gitlab_rails['kerberos_enabled'] = false
        sentinel['enable'] = false
 
        # GitLab Pages
        gitlab_pages['enable'] = false
        pages_nginx['enable'] = false
 
        # 禁用 PUMA 集群模式
        puma['worker_processes'] = 0
        puma['min_threads'] = 1
        puma['max_threads'] = 2
 
        # 降低后台守护进程并发数
        sidekiq['max_concurrency'] = 5
 
        gitlab_ci['gitlab_ci_all_broken_builds'] = false
        gitlab_ci['gitlab_ci_add_pusher'] = false
 
        # 关闭监控
        prometheus_monitoring['enable'] = false
        alertmanager['enable'] = false
        node_exporter['enable'] = false
        redis_exporter['enable'] = false
        postgres_exporter['enable'] = false
        pgbouncer_exporter['enable'] = false
        gitlab_exporter['enable'] = false
        grafana['enable'] = false
        sidekiq['metrics_enabled'] = false
 
 
        # 配置 GitLab 处理内存的方式
        gitlab_rails['env'] = {
          'MALLOC_CONF' =&gt; 'dirty_decay_ms:1000,muzzy_decay_ms:1000'
        }
 
        gitaly['env'] = {
          'MALLOC_CONF' =&gt; 'dirty_decay_ms:1000,muzzy_decay_ms:1000'
        }
    ports:
      - '8088:80'
      - '8443:443'
      - '222:22'
    volumes:
      - '$GITLAB_HOME/gitlab/config:/etc/gitlab'
      - '$GITLAB_HOME/gitlab/logs:/var/log/gitlab'
      - '$GITLAB_HOME/gitlab/data:/var/opt/gitlab'
 
      - type: bind  
        source: /gitlab/license_key.pub   #这个是宿主机的地址
        target: /opt/gitlab/embedded/service/gitlab-rails/.license_encryption_key.pub  #这个是容器里配置文件的地址
      - type: bind  
        source: /gitlab/license.rb   #这个是宿主机的地址
        target: /opt/gitlab/embedded/service/gitlab-rails/ee/app/models/license.rb  #这个是容器里配置文件的地址
 
 
    shm_size: '1g'
</code></pre>
<h2 id="查看密码">查看密码</h2>
<pre><code class="language-shell">cat /etc/gitlab/initial_root_password
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Docker命令]]></title>
        <id>https://yz0812.github.io/post/docker-ming-ling/</id>
        <link href="https://yz0812.github.io/post/docker-ming-ling/">
        </link>
        <updated>2023-05-28T10:28:28.000Z</updated>
        <content type="html"><![CDATA[<h1 id="docker命令">Docker命令</h1>
<p><strong>容器命令</strong></p>
<ul>
<li><code>docker attach</code>：连接到正在运行的容器。</li>
<li><code>docker create</code>：创建一个新的容器但不启动它。</li>
<li><code>docker cp</code>：在容器和主机之间复制文件和文件夹。</li>
<li><code>docker diff</code>：显示容器文件系统中的更改。</li>
<li><code>docker exec</code>：在正在运行的容器中执行新命令。</li>
<li><code>docker export</code>：将容器的文件系统导出为 tar 归档文件。</li>
<li><code>docker inspect</code>：显示有关 Docker 对象（容器、镜像、网络等）的详细信息。</li>
<li><code>docker kill</code>：终止正在运行的容器。</li>
<li><code>docker logs</code>：获取容器的日志。</li>
<li><code>docker pause</code>：暂停一个正在运行的容器。</li>
<li><code>docker port</code>：显示容器的公开端口。</li>
<li><code>docker ps</code>：列出正在运行的容器。</li>
<li><code>docker restart</code>：重启容器。</li>
<li><code>docker rm</code>：删除一个或多个容器。</li>
<li><code>docker start</code>：启动一个或多个已经创建的容器。</li>
<li><code>dockerstop</code>：停止一个或多个正在运行的容器。</li>
<li><code>docker top</code>：显示容器中正在运行的进程。</li>
<li><code>docker unpause</code>：恢复暂停的容器。</li>
<li><code>docker update</code>：更新容器配置。</li>
<li><code>docker wait</code>：阻塞直到容器停止。</li>
</ul>
<p>** 镜像命令**</p>
<ul>
<li><code>docker build</code>：从 Dockerfile 构建镜像。</li>
<li><code>docker commit</code>：从容器创建一个新的镜像。</li>
<li><code>docker history</code>：显示镜像的历史记录。</li>
<li><code>docker images</code>：列出本地镜像。</li>
<li><code>docker import</code>：从归档文件或 URL 创建镜像。</li>
<li><code>docker load</code>：从归档文件中加载镜像。</li>
<li><code>docker pull</code>：从 Docker Hub 或其他 Docker Registry 拉取镜像。</li>
<li><code>docker push</code>：将本地镜像推送到 Docker Hub 或其他 Docker Registry。</li>
<li><code>docker rmi</code>：删除一个或多个镜像。</li>
<li><code>docker save</code>：将镜像保存为归档文件。</li>
<li><code>docker tag</code>：为镜像添加标签。</li>
</ul>
<p>** 网络命令**</p>
<ul>
<li><code>docker network</code>：管理 Docker 网络。</li>
</ul>
<p>** Swarm 命令**</p>
<ul>
<li><code>docker node</code>：管理 Docker 集群节点。</li>
<li><code>docker service</code>：管理 Docker Swarm 服务。</li>
<li><code>docker swarm</code>：管理 Docker Swarm。</li>
</ul>
<p>** 系统命令**</p>
<ul>
<li><code>docker info</code>：显示 Docker 系统信息。</li>
<li><code>docker login</code>：登录到 Docker Hub 或其他 Docker Registry。</li>
<li><code>docker logout</code>：从 Docker Hub 或其他 Docker Registry 注销。</li>
<li><code>docker system</code>：管理 Docker 系统。</li>
<li><code>docker version</code>：显示 Docker 版本信息。</li>
</ul>
<h1 id="docker-compose文件">docker compose文件</h1>
<ul>
<li><code>version</code>：指定 Compose 文件的版本。当前支持的版本为 2.x、3.x、3.1、3.2、3.3 等。</li>
<li><code>services</code>：定义要运行的服务，可以是一个或多个服务。</li>
<li><code>networks</code>：定义要使用的网络，可以是一个或多个网络。</li>
<li><code>volumes</code>：定义要使用的卷，可以是一个或多个卷。</li>
</ul>
<p>除了上述参数外，Docker Compose 文件还支持其他参数，如 <code>environment</code>、<code>ports</code>、<code>depends_on</code>、<code>restart</code>、<code>image</code>、<code>command</code>、<code>volumes</code>、<code>networks</code> 等。这些参数的作用如下：</p>
<ul>
<li><code>environment</code>：设置环境变量，可以是一个列表或一个字典。</li>
<li><code>ports</code>：将容器端口映射到主机端口。</li>
<li><code>depends_on</code>：定义服务之间的依赖关系，以便在启动依赖服务之前启动当前服务。</li>
<li><code>restart</code>：定义容器失败时的重启策略。</li>
<li><code>image</code>：使用的镜像名称和标签。</li>
<li><code>command</code>：在容器中运行的命令，可以覆盖 Dockerfile 中的 <code>CMD</code>。</li>
<li><code>volumes</code>：将主机路径或命名卷挂载到容器中。</li>
<li><code>networks</code>：将服务添加到指定的网络中。</li>
</ul>
<p>除了上述参数外，Docker Compose 文件还支持其他参数，如 <code>network_mode</code>、<code>container_name</code>、<code>links</code>、<code>user</code>、<code>working_dir</code>、<code>entrypoint</code> 等。这些参数的作用如下：</p>
<ul>
<li><code>network_mode</code>：指定容器要使用的网络模式。</li>
<li><code>container_name</code>：指定容器的名称。</li>
<li><code>links</code>：链接到其他服务。</li>
<li><code>user</code>：设置容器中运行进程的用户。</li>
<li><code>working_dir</code>：设置容器工作目录。</li>
<li><code>entrypoint</code>：覆盖 Dockerfile 中的 <code>ENTRYPOINT</code>。</li>
</ul>
<p>Docker Compose 文件还支持通过环境变量文件或命令行参数覆盖 Compose 文件中的参数。例如，可以使用 <code>-f</code> 参数指定 Compose 文件的路径，使用 <code>--project-name</code> 参数指定项目名称，使用 <code>--env-file</code> 参数指定环境变量文件的路径等。</p>
<h1 id="1-env环境变量">1 .env环境变量</h1>
<h2 id="11-定义">1.1 定义</h2>
<blockquote>
<p>在使用 Docker 构建和运行容器时，可以通过使用 <code>.env</code> 文件来设置环境变量和配置参数。<code>.env</code> 文件是一个文本文件，其中包含变量和其对应的值，每行一个变量，格式为 <code>VAR_NAME=VAR_VALUE</code>。</p>
<p>在 Dockerfile 中，可以使用 <code>ENV</code> 命令来设置环境变量，但如果使用 <code>.env</code> 文件，则可以在 Dockerfile 中使用 <code>${VAR_NAME}</code> 的语法来引用变量，例如：</p>
</blockquote>
<pre><code class="language-properties">VAR_NAME=${VAR_NAME}
</code></pre>
<blockquote>
<p>在 Docker Compose 配置文件中，也可以使用 <code>${VAR_NAME}</code> 的语法来引用 <code>.env</code> 文件中的变量。例如：</p>
</blockquote>
<pre><code class="language-yml">version: '3'
services:
  web:
    image: nginx
    environment:
      MY_VAR: ${VAR_NAME}
</code></pre>
<h2 id="12-使用">1.2 使用</h2>
<pre><code class="language-shell"># 构建镜像
docker compose --env-file .env up -d
# .env 文件中的变量仅在构建和运行Docker 容器时才会生效 
# 可以在容器启动命令中使用 -e 参数来将其传递给容器
docker run -e MY_VAR=${VAR_NAME} myimage
# 查看环境变量的引用
docker-compose --env-file ../.env config
</code></pre>
<h1 id="2-docker-compose-up">2. docker compose up</h1>
<h2 id="21-参数">2.1 参数</h2>
<ul>
<li><code>-d</code>：在后台模式下启动容器。</li>
<li><code>--no-color</code>：禁用输出的颜色。</li>
<li><code>--quiet-pull</code>：禁止输出拉取镜像的详细信息。</li>
<li><code>--no-deps</code>：不启动服务依赖的服务。</li>
<li><code>--force-recreate</code>：强制重新创建容器，即使容器已经存在。</li>
<li><code>--always-recreate-deps</code>：重新创建所有服务依赖的容器。</li>
<li><code>--no-recreate</code>：不重新创建容器，即使容器已经存在。</li>
<li><code>--no-build</code>：不构建镜像，直接使用已有的镜像。</li>
<li><code>--build</code>：在启动容器之前构建镜像。</li>
<li><code>--abort-on-container-exit</code>：当容器退出时，停止所有容器的运行。</li>
<li><code>--attach-dependencies</code>：在启动容器之前启动所有服务依赖的服务。</li>
<li><code>--remove-orphans</code>：移除无关的容器，即没有被 Compose 文件中定义的服务所依赖的容器。</li>
<li><code>--timeout TIMEOUT</code>：设置容器启动的超时时间，默认为 10 秒。</li>
<li><code>--renew-anon-volumes</code>：在启动容器之前删除匿名卷，并重新创建它们。</li>
<li><code>--remove-volumes</code>：在移除容器时同时移除关联的匿名和命名卷。</li>
<li><code>--volumes</code>：在容器之间共享命名卷。</li>
<li><code>--scale SERVICE=NUM</code>：指定要启动的服务以及要启动的容器数量，例如 <code>--scale web=2</code> 表示启动 <code>web</code> 服务的 2 个容器。</li>
<li><code>--no-log-prefix</code>：不在日志中输出服务名称的前缀。</li>
<li><code>--log-level LEVEL</code>：设置日志的级别，默认为 <code>info</code>。</li>
<li><code>--exit-code-from SERVICE</code>：从指定的服务获取退出代码，并在所有容器停止时退出。</li>
<li><code>--disable-content-trust</code>：禁用 Docker Content Trust。</li>
<li><code>--pull</code>：在启动容器之前拉取最新的镜像。</li>
<li><code>--no-healthcheck</code>：不运行健康检查。</li>
<li><code>--compose-file FILE</code>：指定要使用的 Compose 文件，可以指定多个文件。</li>
<li><code>--project-namePROJECT_NAME</code>：指定 Compose 项目的名称。</li>
<li><code>-f, --file FILE</code>：指定要使用的 Compose 文件，可以指定多个文件。</li>
<li><code>--env-file PATH</code>：指定要使用的环境变量文件。</li>
<li><code>--env, -e KEY=VALUE</code>：设置环境变量，可以指定多个。</li>
<li><code>--no-recreate</code>：不重新创建容器，即使容器已经存在。</li>
<li><code>--no-start</code>：不启动容器，只构建镜像。</li>
<li><code>--scale SERVICE=NUM</code>：指定要启动的服务以及要启动的容器数量，例如 <code>--scale web=2</code> 表示启动 <code>web</code> 服务的 2 个容器。</li>
<li><code>--remove-orphans</code>：移除无关的容器，即没有被 Compose 文件中定义的服务所依赖的容器。</li>
<li><code>--no-build</code>：不构建镜像，直接使用已有的镜像。</li>
<li><code>--build</code>：在启动容器之前构建镜像。</li>
<li><code>--always-recreate-deps</code>：重新创建所有服务依赖的容器。</li>
<li><code>--force-recreate</code>：强制重新创建容器，即使容器已经存在。</li>
<li><code>--no-deps</code>：不启动服务依赖的服务。</li>
<li><code>--abort-on-container-exit</code>：当容器退出时，停止所有容器的运行。</li>
<li><code>--attach-dependencies</code>：在启动容器之前启动所有服务依赖的服务。</li>
<li><code>--remove-orphans</code>：移除无关的容器，即没有被 Compose 文件中定义的服务所依赖的容器。</li>
<li><code>--timeout TIMEOUT</code>：设置容器启动的超时时间，默认为 10 秒。</li>
<li><code>--renew-anon-volumes</code>：在启动容器之前删除匿名卷，并重新创建它们。</li>
<li><code>--remove-volumes</code>：在移除容器时同时移除关联的匿名和命名卷。</li>
<li><code>--volumes</code>：在容器之间共享命名卷。</li>
<li><code>--no-log-prefix</code>：不在日志中输出服务名称的前缀。</li>
<li><code>--log-level LEVEL</code>：设置日志的级别，默认为 <code>info</code>。</li>
<li><code>--exit-code-from SERVICE</code>：从指定的服务获取退出代码，并在所有容器停止时退出。</li>
<li><code>--disable-content-trust</code>：禁用 Docker Content Trust。</li>
</ul>
<h2 id="22-举例">2.2 举例</h2>
<pre><code class="language-shell"># 指定一个镜像
docker compose up -d redis
# 指定环境变量
docker-compose --env-file ../.env up -d
# 重新创建容器，即使容器已经存在。
docker compose up   --force-recreate -d
# 构建镜像并启动容器。
docker-compose up --build
# 指定使用的 Compose 文件，默认文件名为 docker-compose.yml。
docker-compose -f filename.yml up
# 指定项目名称。
docker-compose -p projectname up
# 如果一个容器退出，则停止所有容器。
docker-compose up --abort-on-container-exit

</code></pre>
<h1 id="3docker-logs">3.docker logs</h1>
<h2 id="31参数">3.1参数</h2>
<ul>
<li><code>-f</code>：实时输出日志，类似于 <code>tail -f</code> 命令。</li>
<li><code>--tail NUM</code>：仅输出最后 NUM 行的日志，默认为全部输出。</li>
<li><code>--since TIME</code>：仅输出从指定时间点开始的日志，例如 <code>2022-05-01T00:00:00</code>。</li>
<li><code>--until TIME</code>：仅输出到指定时间点为止的日志，例如 <code>2022-05-01T23:59:59</code>。</li>
<li><code>--details</code>：输出更多的容器详细信息，例如容器的标签、ID、名称等。</li>
<li><code>--timestamps</code>：在日志中显示时间戳。</li>
<li><code>--follow</code> 或 <code>-f</code>：在输出日志后保持连接，实时查看容器日志的变化。</li>
<li><code>--tail</code> 或 <code>-n</code>：指定输出日志的行数。</li>
<li><code>--since</code>：指定输出日志的起始时间。</li>
</ul>
<h2 id="32-举例">3.2 举例</h2>
<h3 id="321-查看容器的所有日志">3.2.1 查看容器的所有日志：</h3>
<pre><code class="language-shell">docker logs &lt;container_id&gt;
</code></pre>
<h3 id="322-查看容器的最后-100-行日志">3.2.2 查看容器的最后 100 行日志：</h3>
<pre><code class="language-shell">docker logs --tail 100 &lt;container_id&gt;
</code></pre>
<h3 id="323-查看容器的实时日志">3.2.3 查看容器的实时日志：</h3>
<pre><code class="language-shell">docker logs &lt;container_id&gt; 2&gt;&amp;1 | grep &quot;error&quot;
</code></pre>
<h3 id="324-查看容器的所有日志并在其中搜索关键字-error">3.2.4 查看容器的所有日志，并在其中搜索关键字 <code>error</code>：</h3>
<pre><code class="language-shell">docker logs -f &lt;container_id&gt;
</code></pre>
<h3 id="325-查看容器的最后-100-行日志并在其中搜索关键字-error">3.2.5 查看容器的最后 100 行日志，并在其中搜索关键字 <code>error</code>：</h3>
<pre><code class="language-shell">docker logs --tail 100 &lt;container_id&gt; 2&gt;&amp;1 | grep &quot;error&quot;
</code></pre>
<h3 id="326-查看容器的实时日志并在其中搜索关键字-error">3.2.6 查看容器的实时日志，并在其中搜索关键字 <code>error</code>：：</h3>
<pre><code class="language-shell">docker logs -f &lt;container_id&gt; 2&gt;&amp;1 | grep &quot;error&quot;
</code></pre>
<h1 id="4docker-inspect">4.Docker Inspect</h1>
<p><code>docker inspect</code> 命令用于获取 Docker 对象（容器、镜像、网络等）</p>
<h2 id="41-参数">4.1 参数</h2>
<ul>
<li><code>--format</code>: 指定输出的格式。</li>
<li><code>--type</code>: 指定要检查的对象类型。</li>
<li><code>--size</code>: 显示对象占用的磁盘空间大小。</li>
<li><code>--all</code>: 显示所有对象的详细信息。</li>
<li><code>--verbose</code>: 显示更详细的信息。</li>
<li><code>--pretty</code>: 显示格式化的输出。</li>
<li><code>--no-trunc</code>: 显示完整的输出，而不是截断后的输出。</li>
<li><code>--help</code>: 显示帮助信息。</li>
</ul>
<h2 id="42-举例">4.2 举例</h2>
<h3 id="421-检查容器的详细信息">4.2.1 检查容器的详细信息：</h3>
<pre><code class="language-shell">docker inspect  &lt;container_id&gt; 
</code></pre>
<h3 id="422-检查容器-ip-地址">4.2.2 检查容器 IP 地址：</h3>
<pre><code class="language-shell">docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'  &lt;container_id&gt; 
</code></pre>
<h3 id="423-检查镜像信息">4.2.3 检查镜像信息：</h3>
<pre><code class="language-shell">docker inspect &lt;image_id&gt; 
</code></pre>
<h3 id="424-检查网络信息">4.2.4 检查网络信息：</h3>
<pre><code class="language-shell">docker inspect &lt;network_name&gt;
</code></pre>
<h3 id="425-指定输出格式">4.2.5 指定输出格式：</h3>
<pre><code class="language-shell">docker inspect --format='{{.Name}} - {{.State.Status}}' &lt;container_id&gt; 
</code></pre>
<h3 id="426-按条件筛选容器">4.2.6 按条件筛选容器：</h3>
<pre><code class="language-shell">docker inspect -f '{{.Name}} {{.State.Running}}' $(docker ps -q --filter &quot;status=running&quot;)
</code></pre>
<h3 id="427-按条件筛选镜像">4.2.7 按条件筛选镜像：</h3>
<pre><code class="language-shell">docker inspect -f '{{.RepoTags}} {{.Size}}' $(docker images -q --filter &quot;dangling=true&quot;)
</code></pre>
<h3 id="428-使用jq格式化工具">4.2.8 使用jq格式化工具</h3>
<pre><code class="language-shell">docker inspect  &lt;container_id&gt;   | jq '.[0] | {id: .Id, created: .Created, author: .Author, size: .Size, labels: .Config.Labels}'
</code></pre>
<h3 id="429-查看目录挂载信息">4.2.9 查看目录挂载信息</h3>
<pre><code class="language-shell">docker inspect --format=&quot;{{json .Mounts}}&quot; &lt;container_id&gt; | jq
</code></pre>
<h3 id="4210-查看容器网络信息">4.2.10 查看容器网络信息</h3>
<pre><code class="language-shell">#查看完整网络信息
docker inspect --format=&quot;{{json .NetworkSettings}}&quot; &lt;container_id&gt; | jq
#查看网络端口映射
docker inspect --format=&quot;{{json .NetworkSettings.Ports}}&quot; &lt;container_id&gt; | jq
# 查看容器的网络ip、网关等信息
docker inspect --format=&quot;{{json .NetworkSettings.Networks}}&quot; &lt;container_id&gt; | jq

</code></pre>
<h3 id="4211-查看容器的端口映射">4.2.11 查看容器的端口映射：</h3>
<pre><code class="language-shell">docker inspect --format='{{json .HostConfig.PortBindings}}' mycontainer
</code></pre>
<h3 id="4212-查看容器的环境变量">4.2.12 查看容器的环境变量：</h3>
<pre><code class="language-shell">docker inspect --format='{{json .Config.Env}}' mycontainer
</code></pre>
<h3 id=""></h3>
<h1 id="5docker-exec">5.docker exec</h1>
<h2 id="51-参数">5.1 参数</h2>
<ul>
<li><code>-it</code>: 以交互模式进入容器中执行命令，通常用于需要交互式操作的命令。</li>
<li><code>-d</code>: 让容器在后台运行。</li>
<li><code>--user</code>: 指定要执行命令的用户。</li>
<li><code>--workdir</code>: 指定命令执行的工作目录。</li>
<li><code>--env</code>: 设置环境变量。</li>
<li><code>--privileged</code>: 让容器拥有特权，可以访问主机的设备和文件系统。</li>
<li><code>--name</code>: 指定容器的名称。</li>
<li><code>--detach-keys</code>: 指定在后台运行容器时使用的键盘序列。</li>
</ul>
<h2 id="52-举例">5.2 举例</h2>
<h3 id="521-进入正在运行的容器并执行命令">5.2.1 进入正在运行的容器并执行命令：</h3>
<pre><code class="language-shell">docker exec -it mycontainer sh
</code></pre>
<p>上面的命令将以交互模式进入名为 <code>mycontainer</code> 的容器，并在容器中执行 <code>sh</code> 命令。</p>
<h3 id="522-在后台运行容器并执行命令">5.2.2 在后台运行容器并执行命令：</h3>
<pre><code class="language-shell">docker exec -d mycontainer ls
</code></pre>
<p>上面的命令将在名为 <code>mycontainer</code> 的容器中运行 <code>ls</code> 命令，并让容器在后台运行。</p>
<h3 id="523-指定执行命令的用户">5.2.3 指定执行命令的用户：</h3>
<pre><code class="language-shell">docker exec --user=username mycontainer whoami
</code></pre>
<p>上面的命令将在名为 <code>mycontainer</code> 的容器中以 <code>username</code> 用户身份执行 <code>whoami</code> 命令。</p>
<h3 id="524-指定命令执行的工作目录">5.2.4 指定命令执行的工作目录：</h3>
<pre><code class="language-shell">docker exec --workdir=/app mycontainer ls
</code></pre>
<p>上面的命令将在名为 <code>mycontainer</code> 的容器中以 <code>/app</code> 目录作为工作目录执行 <code>ls</code> 命令。</p>
<h3 id="525-设置环境变量并执行命令">5.2.5 设置环境变量并执行命令：</h3>
<pre><code class="language-shell">docker exec --env MY_VAR=value mycontainer env
</code></pre>
<p>上面的命令将在名为 <code>mycontainer</code> 的容器中设置名为 <code>MY_VAR</code> 的环境变量为 <code>value</code>，并执行 <code>env</code> 命令。</p>
<h3 id="526-让容器拥有特权并执行命令">5.2.6 让容器拥有特权并执行命令：</h3>
<pre><code class="language-shell">docker exec --privileged mycontainer ls /dev
</code></pre>
<p>上面的命令将在名为 <code>mycontainer</code> 的容器中以特权模式运行 <code>ls /dev</code> 命令，可以访问主机的设备和文件系统。</p>
<h3 id="527-指定容器的名称并执行命令">5.2.7 指定容器的名称并执行命令：</h3>
<pre><code class="language-shell">docker exec --name=myexec mycontainer ls
</code></pre>
<p>上面的命令将在名为 <code>mycontainer</code> 的容器中执行 <code>ls</code> 命令，并将执行结果输出到名为 <code>myexec</code> 的容器中。</p>
<h3 id="528-指定在后台运行容器时使用的键盘序列">5.2.8 指定在后台运行容器时使用的键盘序列：</h3>
<pre><code class="language-shell">docker exec --detach-keys=&quot;ctrl-d&quot; mycontainer ls
</code></pre>
<p>上面的命令将在名为 <code>mycontainer</code> 的容器中运行 <code>ls</code> 命令，并在后台运行容器时使用 <code>ctrl-d</code> 键作为键盘序列。</p>
<h3 id="529-在容器中查看日志">5.2.9 在容器中查看日志：</h3>
<pre><code class="language-shell">docker exec -it mycontainer cat /var/log/mylog.log
</code></pre>
<h3 id="5210-查看容器内的进程">5.2.10 查看容器内的进程：</h3>
<pre><code class="language-shell">dockerexec -it mycontainer ps -ef
</code></pre>
<h1 id="6-docker-cp">6. docker cp</h1>
<h2 id="61-参数">6.1 参数</h2>
<ul>
<li><code>-a, --archive</code>: 以归档模式进行复制，包括复制文件的所有 uid/gid 信息。</li>
<li><code>--follow-link</code>: 始终跟随源路径中的符号链接。</li>
<li><code>--help</code>: 显示帮助信息。</li>
</ul>
<h2 id="62-举例">6.2 举例</h2>
<h3 id="621-从容器复制文件到主机">6.2.1. 从容器复制文件到主机：</h3>
<pre><code class="language-shell">docker cp mycontainer:/path/to/myfile.txt /host/path/
</code></pre>
<p>上面的命令将从名为 <code>mycontainer</code> 的容器中复制 <code>/path/to/myfile.txt</code> 文件到主机的 <code>/host/path/</code> 目录中。</p>
<h3 id="622-从主机复制文件到容器">6.2.2 从主机复制文件到容器：</h3>
<pre><code class="language-shell">docker cp /host/path/myfile.txt mycontainer:/path/to/
</code></pre>
<p>上面的命令将从主机的 <code>/host/path/myfile.txt</code> 文件复制到名为 <code>mycontainer</code> 的容器中的 <code>/path/to/</code> 目录中。</p>
<h3 id="623-使用-a-参数复制所有的文件属性">6.2.3 使用 <code>-a</code> 参数复制所有的文件属性：</h3>
<pre><code class="language-shell">docker cp -a mycontainer:/path/to/myfile.txt /host/path/
</code></pre>
<p>上面的命令将从名为 <code>mycontainer</code> 的容器中复制 <code>/path/to/myfile.txt</code> 文件到主机的 <code>/host/path/</code> 目录中，并保留文件的所有属性。</p>
<h3 id="624-使用-follow-link-参数跟随符号链接">6.2.4 使用 <code>-follow-link</code> 参数跟随符号链接：</h3>
<pre><code class="language-shell">docker cp --follow-link mycontainer:/path/to/mylink /host/path/
</code></pre>
<p>上面的命令将从名为 <code>mycontainer</code> 的容器中复制 <code>/path/to/mylink</code> 符号链接所指向的文件到主机的 <code>/host/path/</code> 目录中。</p>
<h3 id="625-使用-作为目标路径打印到标准输出">6.2.5 使用 <code>-</code> 作为目标路径打印到标准输出：</h3>
<pre><code class="language-shell">docker cp mycontainer:/path/to/myfile.txt -
</code></pre>
<p>上面的命令将从名为 <code>mycontainer</code> 的容器中读取 <code>/path/to/myfile.txt</code> 文件的内容，并输出到标准输出。</p>
<h3 id="626-从容器中复制整个目录">6.2.6 从容器中复制整个目录：</h3>
<pre><code class="language-shell">docker cp mycontainer:/path/to/mydir /host/path/
</code></pre>
<p>上面的命令将从名为 <code>mycontainer</code> 的容器中复制 <code>/path/to/mydir</code> 目录及其下的所有文件和子目录到主机的 <code>/host/path/</code> 目录中。</p>
<h3 id="627-在容器中创建新文件并复制">6.2.7 在容器中创建新文件并复制：</h3>
<pre><code class="language-shell">docker cp /host/path/myfile.txt mycontainer:/path/to/newfile.txt
</code></pre>
<p>上面的命令将在名为 <code>mycontainer</code> 的容器中创建名为 <code>/path/to/newfile.txt</code> 的新文件，并将主机中的 <code>/host/path/myfile.txt</code> 文件复制到新文件中。</p>
<p>这些是 <code>docker cp</code> 常用的参数和使用示例，可以根据具体的应用场景和需求，使用不同的参数和路径进行复制操作。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JavaCV 视频流抽帧]]></title>
        <id>https://yz0812.github.io/post/javacv-shi-pin-liu-chou-zheng/</id>
        <link href="https://yz0812.github.io/post/javacv-shi-pin-liu-chou-zheng/">
        </link>
        <updated>2022-12-09T16:07:29.000Z</updated>
        <content type="html"><![CDATA[<p>pom文件引入依赖：</p>
<pre><code class="language-java">&lt;dependencies&gt;
    &lt;dependency&gt;
        &lt;groupId&gt;org.bytedeco&lt;/groupId&gt;
        &lt;artifactId&gt;javacv-platform&lt;/artifactId&gt;
        &lt;version&gt;1.5.7&lt;/version&gt;
    &lt;/dependency&gt;
&lt;/dependencies&gt;
</code></pre>
<pre><code class="language-java">@Slf4j
public class OpencvUtil {

    public static BufferedImage FrameToBufferedImage(org.bytedeco.javacv.Frame frame) {
        Java2DFrameConverter converter = new Java2DFrameConverter();
        BufferedImage bufferedImage = converter.getBufferedImage(frame);
//      bufferedImage=rotateClockwise90(bufferedImage);
        return bufferedImage;
    }

    /**
     * 处理图片，将图片旋转90度。
     */
    public static BufferedImage rotateClockwise90(BufferedImage bi) {
        int width = bi.getWidth();
        int height = bi.getHeight();
        BufferedImage bufferedImage = new BufferedImage(height, width, bi.getType());
        for (int i = 0; i &lt; width; i++)
            for (int j = 0; j &lt; height; j++)
                bufferedImage.setRGB(j, i, bi.getRGB(i, j));
        return bufferedImage;
    }
  
      /**
     * @param url             url
     * @param filePath        文件路径
     * @param fileName        文件名
     * @param minioProperties 迷你属性
     * @return {@link String }
     * @description 切割框架
     * @author yz
     * @date 2022/12/08
     **/
    public static String cutFrame(String url, String fileName) {
        FFmpegFrameGrabber grabber;
        try {
            grabber = FFmpegFrameGrabber.createDefault(url);
            // 使用tcp的方式，不然会丢包很严重
            grabber.setOption(&quot;rtsp_transport&quot;, &quot;tcp&quot;);
            // 图片大小
            grabber.setImageWidth(1280);
            grabber.setImageHeight(720);
            grabber.start();
          
            //截帧
            File outPut = new File(fileName);
            org.bytedeco.javacv.Frame frame = grabber.grabImage();
            if (frame != null) {
                ImageIO.write(OpencvUtil.FrameToBufferedImage(frame), &quot;jpeg&quot;, outPut);
            }
            grabber.stop();
            grabber.release();
           } catch (Exception e) {
            System.out.println(e);
        }
   }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ Flyway指南]]></title>
        <id>https://yz0812.github.io/post/flyway-zhi-nan/</id>
        <link href="https://yz0812.github.io/post/flyway-zhi-nan/">
        </link>
        <updated>2022-11-06T10:56:05.000Z</updated>
        <content type="html"><![CDATA[<h3 id="flyway指南">Flyway指南</h3>
<p>flyway工作流程如下：</p>
<ol>
<li>项目启动，应用程序完成数据库连接池的建立后，Flyway自动运行。</li>
<li>初次使用时，flyway会创建一个 flyway_schema_history 表，用于记录sql执行记录。</li>
<li>Flyway会扫描项目指定路径下(默认是 classpath:db/migration )的所有sql脚本，与 flyway_schema_history 表脚本记录进行比对。如果数据库记录执行过的脚本记录，与项目中的sql脚本不一致，Flyway会报错并停止项目执行。</li>
<li>如果校验通过，则根据表中的sql记录最大版本号，忽略所有版本号不大于该版本的脚本。再按照版本号从小到大，逐个执行其余脚本。</li>
</ol>
<p>让我们开始使用Flyway进行数据库管理.</p>
<h2 id="1配置">1.配置</h2>
<blockquote>
<p>创建一个springboot的项目,添加maven插件</p>
</blockquote>
<pre><code class="language-xml">			&lt;plugin&gt;
				&lt;groupId&gt;org.flywaydb&lt;/groupId&gt;
				&lt;artifactId&gt;flyway-maven-plugin&lt;/artifactId&gt;
      &lt;/plugin&gt;
</code></pre>
<blockquote>
<p>导包</p>
</blockquote>
<pre><code class="language-xml">&lt;dependency&gt;
   &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
   &lt;artifactId&gt;spring-boot-starter-jdbc&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
   &lt;groupId&gt;mysql&lt;/groupId&gt;
   &lt;artifactId&gt;mysql-connector-java&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
   &lt;groupId&gt;org.flywaydb&lt;/groupId&gt;
   &lt;artifactId&gt;flyway-core&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
   &lt;groupId&gt;org.flywaydb&lt;/groupId&gt;
   &lt;artifactId&gt;flyway-mysql&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
   &lt;groupId&gt;org.projectlombok&lt;/groupId&gt;
   &lt;artifactId&gt;lombok&lt;/artifactId&gt;
   &lt;optional&gt;true&lt;/optional&gt;
&lt;/dependency&gt;
</code></pre>
<blockquote>
<p>application配置文件</p>
</blockquote>
<pre><code class="language-yaml">spring:
  flyway:
    # 文件位置
    locations: classpath:db/sql
    # 是否启用
    enabled: true
    # 文件版本
    baseline-version: 1
    # 文件前缀 社区版会报错
    # baselineMigrationPrefix is not supported by Flyway Community Edition Try Flyway Teams Edition for free: https://rd.gt/2VzHpkY
    # baseline-migration-prefix: V
    # 在没有元数据表的情况下，针对非空Schema执行迁移时是否自动调用基线。 （默认值：false）
    baseline-on-migrate: true
    # 迁移时是否进行校验，默认true
    validate-on-migrate: true
    # 编码格式，默认UTF-8
    encoding: UTF-8
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${mysql.ip}:${mysql.port}/flyway?useUnicode=true&amp;characterEncoding=UTF-8&amp;useJDBCCompliantTimezoneShift=true&amp;useLegacyDatetimeCode=false&amp;serverTimezone=GMT%2b8
    username: ${mysql.username}
    password: ${mysql.password}
</code></pre>
<h2 id="2定义第一次迁移">2.定义第一次迁移</h2>
<p>可以通过配置看到默认的路径是<code>classpath:db/migration</code>但是上面我们通过locations的配置改成了db/sql,所以我们在resources文件夹中创建目录db/sql</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/yz0812/mypic/master/202211061802203.png" alt="" loading="lazy"></figure>
<p>Flyway 遵循以下<strong>迁移脚本命名约定：</strong></p>
<p><em>&lt;前缀&gt;&lt;版本&gt;__&lt;说明&gt;.sql</em></p>
<ul>
<li><em>&lt;前缀&gt;</em>–默认前缀是<em>V</em>，我们可以在上面的配置文件中使用<em>flyway.sqlMigrationPrefix</em>属性进行更改。</li>
<li><em>&lt;版本&gt;</em> – 迁移版本号。主要版本和次要版本可以用<em>下划线</em>分隔。迁移版本应始终以 1 开头。</li>
<li><em>&lt;描述&gt;</em> – 迁移的文本描述。双下划线将说明与版本号分开。</li>
</ul>
<p>示例：<em>V1.0.0__Base_version.sql</em></p>
<p>现在让我们在项目的 resources/db/sql目录下面创建一个文件夹 1.0.0,再创建一个名为V1.0.0__Base_version.sql的迁移脚本</p>
<pre><code class="language-sql">DROP TABLE IF EXISTS user ;
CREATE TABLE `user` (
                        `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
                        `name` varchar(20) NOT NULL COMMENT '姓名',
                        `age` int DEFAULT NULL COMMENT '年龄',
                        PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=4 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;


DROP TABLE IF EXISTS student ;
CREATE TABLE `student` (
                           `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
                           `name` varchar(20) NOT NULL COMMENT '姓名',
                           `age` int DEFAULT NULL COMMENT '年龄',
                           PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=4 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;

</code></pre>
<p>这个时候启动springboot项目,就可以在数据库中看到三个表<code>flyway_schema_history</code>,<code>user</code>,<code>student</code>;</p>
<h2 id="3定义第二次迁移">3.定义第二次迁移</h2>
<p>在项目的 resources/db/sql目录下面创建一个文件夹 1.1.0,再创建一个名为V1.1.0__20221106_insert.sql的迁移脚本</p>
<pre><code class="language-sql">insert into user(name,age) values ('张三',13);
insert into user(name,age) values ('李四',14);
insert into user(name,age) values ('王五',15);
</code></pre>
<p>启动springboot项目,就可以在数据库user表中看到插入的数据,而且还可以看到<code>flyway_schema_history</code>表多了两行数据对应我们刚刚的两个迁移脚本</p>
<pre><code class="language-tex">installed_rank|version|description    |type|script                            |checksum   |installed_by|installed_on       |execution_time|success|
--------------+-------+---------------+----+----------------------------------+-----------+------------+-------------------+--------------+-------+
             1|1.0.0  |Base version   |SQL |v1.0.0/V1.0.0__Base_version.sql   | -852860158|root        |2022-11-06 10:10:47|           520|      1|
             2|1.1.0  |20221106 insert|SQL |v1.1.0/V1.1.0__20221106_insert.sql|-1054580649|root        |2022-11-06 10:10:48|           306|      1|
</code></pre>
<h2 id="4定义第三次迁移">4.定义第三次迁移</h2>
<p>在项目的 resources/db/sql目录下面创建一个文件夹1.2.0,再创建一个名为V1.2.0__20221106_insert.sql的迁移脚本</p>
<pre><code class="language-sql">insert into student(name,age)values ('张三',13);
insert into student(name,age)values ('李四',14);
insert into student(name,age)values ('王五',15);
</code></pre>
<p>启动springboot项目,就可以在数据库student表中看到插入的数据</p>
<h2 id="5定义第五次迁移">5.定义第五次迁移</h2>
<p>我们更改db/sql/v1.2.0/V1.2.0__20221106_insert.sql这个文件,新加一条数据</p>
<pre><code class="language-sql">insert into student(name,age)values ('张三',13);
insert into student(name,age)values ('李四',14);
insert into student(name,age)values ('王五',15);
insert into student(name,age)values ('刘六',16);
</code></pre>
<p>启动springboot项目,我们就会得到一个错误<code>Validate failed: Migrations have failed validation</code>,这是因为检查到<code>V1.2.0__20221106_insert.sql</code>已经发生改变了与<code>flyway_schema_history</code> checksum的不一致</p>
<pre><code class="language-tcl">
org.springframework.beans.factory.BeanCreationException: Error creating bean with name 'flywayInitializer' defined in class path resource [org/springframework/boot/autoconfigure/flyway/FlywayAutoConfiguration$FlywayConfiguration.class]: Invocation of init method failed; nested exception is org.flywaydb.core.api.exception.FlywayValidateException: Validate failed: Migrations have failed validation
Migration checksum mismatch for migration version 1.2.0
-&gt; Applied to database : -860246032
-&gt; Resolved locally    : 461714632
Either revert the changes to the migration, or run repair to update the schema history.
Need more flexibility with validation rules? Learn more: https://rd.gt/3AbJUZE
	at org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory.initializeBean(AbstractAutowireCapableBeanFactory.java:1804) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory.doCreateBean(AbstractAutowireCapableBeanFactory.java:620) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory.createBean(AbstractAutowireCapableBeanFactory.java:542) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.AbstractBeanFactory.lambda$doGetBean$0(AbstractBeanFactory.java:335) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.DefaultSingletonBeanRegistry.getSingleton(DefaultSingletonBeanRegistry.java:234) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.AbstractBeanFactory.doGetBean(AbstractBeanFactory.java:333) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.AbstractBeanFactory.getBean(AbstractBeanFactory.java:208) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.AbstractBeanFactory.doGetBean(AbstractBeanFactory.java:322) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.AbstractBeanFactory.getBean(AbstractBeanFactory.java:208) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.DefaultListableBeanFactory.preInstantiateSingletons(DefaultListableBeanFactory.java:955) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.context.support.AbstractApplicationContext.finishBeanFactoryInitialization(AbstractApplicationContext.java:918) ~[spring-context-5.3.23.jar:5.3.23]
	at org.springframework.context.support.AbstractApplicationContext.refresh(AbstractApplicationContext.java:583) ~[spring-context-5.3.23.jar:5.3.23]
	at org.springframework.boot.SpringApplication.refresh(SpringApplication.java:734) [spring-boot-2.7.5.jar:2.7.5]
	at org.springframework.boot.SpringApplication.refreshContext(SpringApplication.java:408) [spring-boot-2.7.5.jar:2.7.5]
	at org.springframework.boot.SpringApplication.run(SpringApplication.java:308) [spring-boot-2.7.5.jar:2.7.5]
	at org.springframework.boot.SpringApplication.run(SpringApplication.java:1306) [spring-boot-2.7.5.jar:2.7.5]
	at org.springframework.boot.SpringApplication.run(SpringApplication.java:1295) [spring-boot-2.7.5.jar:2.7.5]
	at com.example.flyway.FlywayApplication.main(FlywayApplication.java:10) [classes/:na]
Caused by: org.flywaydb.core.api.exception.FlywayValidateException: Validate failed: Migrations have failed validation
Migration checksum mismatch for migration version 1.2.0
-&gt; Applied to database : -860246032
-&gt; Resolved locally    : 461714632
Either revert the changes to the migration, or run repair to update the schema history.
Need more flexibility with validation rules? Learn more: https://rd.gt/3AbJUZE
	at org.flywaydb.core.Flyway$1.execute(Flyway.java:130) ~[flyway-core-8.5.13.jar:na]
	at org.flywaydb.core.Flyway$1.execute(Flyway.java:124) ~[flyway-core-8.5.13.jar:na]
	at org.flywaydb.core.FlywayExecutor.execute(FlywayExecutor.java:205) ~[flyway-core-8.5.13.jar:na]
	at org.flywaydb.core.Flyway.migrate(Flyway.java:124) ~[flyway-core-8.5.13.jar:na]
	at org.springframework.boot.autoconfigure.flyway.FlywayMigrationInitializer.afterPropertiesSet(FlywayMigrationInitializer.java:66) ~[spring-boot-autoconfigure-2.7.5.jar:2.7.5]
	at org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory.invokeInitMethods(AbstractAutowireCapableBeanFactory.java:1863) ~[spring-beans-5.3.23.jar:5.3.23]
	at org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory.initializeBean(AbstractAutowireCapableBeanFactory.java:1800) ~[spring-beans-5.3.23.jar:5.3.23]
	... 17 common frames omitted
</code></pre>
<p>解决办法</p>
<ul>
<li>手动修正这一条数据,但是改动的数据不会执行,相当于跳过. <strong>update</strong> flyway_schema_history <strong>set</strong> checksum = 461714632 <strong>where</strong> <code>version</code> = '1.2.0';</li>
<li>删除这条数据 <strong>delete</strong> <strong>from</strong> flyway_schema_history <strong>where</strong> <strong>version</strong> = '1.2.0';</li>
</ul>
<h2 id="6重复运行的sql">6.重复运行的SQL</h2>
<p>可重复运行的SQL，则以大写的“R”开头，后面再以两个下划线分割，其后跟文件名称例如:<code>R__empty_data.sql</code> 他是不会被flyway检测的所以在<code>flyway_schema_history</code>中他的<code>checksum</code>的值是0;</p>
<h2 id="7插件管理">7.插件管理</h2>
<p>配置插件</p>
<pre><code class="language-xml">			&lt;plugin&gt;
				&lt;groupId&gt;org.flywaydb&lt;/groupId&gt;
				&lt;artifactId&gt;flyway-maven-plugin&lt;/artifactId&gt;
				&lt;configuration&gt;
					&lt;user&gt;${mysql.username}&lt;/user&gt;
					&lt;password&gt;${mysql.password}&lt;/password&gt;
					&lt;url&gt;jdbc:mysql://${mysql.ip}:${mysql.port}/flyway?useUnicode=true&amp;characterEncoding=UTF-8&amp;useJDBCCompliantTimezoneShift=true&amp;useLegacyDatetimeCode=false&amp;serverTimezone=GMT%2b8&lt;/url&gt;
					&lt;locations&gt;
						&lt;location&gt;classpath:db/sql&lt;/location&gt;
					&lt;/locations&gt;
					&lt;schemas&gt;
						&lt;schema&gt;flyway&lt;/schema&gt;
					&lt;/schemas&gt;
				&lt;/configuration&gt;
			&lt;/plugin&gt;
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/yz0812/mypic/master/202211061852000.png" alt="" loading="lazy"></figure>
<p>命令:</p>
<ol>
<li>
<p>baseline</p>
<p>对已经存在数据库Schema结构的数据库一种解决方案。实现在非空数据库新建MetaData表，并把Migrations应用到该数据库；也可以在已有表结构的数据库中实现添加Metadata表。</p>
</li>
<li>
<p>clean</p>
<p>清除掉对应数据库Schema中所有的对象，包括表结构，视图，存储过程等，clean操作在dev 和 test阶段很好用，但在生产环境务必禁用。</p>
</li>
<li>
<p>info</p>
<p>用于打印所有的Migrations的详细和状态信息，也是通过MetaData和Migrations完成的，可以快速定位当前的数据库版本。</p>
</li>
<li>
<p>repair</p>
<p>repair操作能够修复metaData表，该操作在metadata出现错误时很有用。</p>
</li>
<li>
<p>undo</p>
<p>撤销操作，社区版不支持。</p>
</li>
<li>
<p>validate</p>
<p>验证已经apply的Migrations是否有变更，默认开启的，原理是对比MetaData表与本地Migrations的checkNum值，如果值相同则验证通过，否则失败。</p>
</li>
</ol>
<h2 id="7flyway知识补充">7.flyway知识补充</h2>
<ul>
<li>flyway执行migrate必须在空白的数据库上进行，否则报错。</li>
<li>对于已经有数据的数据库，必须先baseline，然后才能migrate。</li>
<li>clean操作是删除数据库的所有内容，包括baseline之前的内容。</li>
<li>尽量不要修改已经执行过的SQL，即便是R开头的可反复执行的SQL，它们会不利于数据迁移。</li>
<li>当需要做数据迁移的时候，更换一个新的空白数据库，执行下migrate命令，所有的数据库更改都可以一步到位地迁移过去。</li>
</ul>
<h2 id="8flyway配置清单">8.flyway配置清单</h2>
<pre><code class="language-yaml">flyway.baseline-description对执行迁移时基准版本的描述.
flyway.baseline-on-migrate当迁移时发现目标schema非空，而且带有没有元数据的表时，是否自动执行基准迁移，默认false.
flyway.baseline-version开始执行基准迁移时对现有的schema的版本打标签，默认值为1.
flyway.check-location检查迁移脚本的位置是否存在，默认false.
flyway.clean-on-validation-error当发现校验错误时是否自动调用clean，默认false.
flyway.enabled是否开启flywary，默认true.
flyway.encoding设置迁移时的编码，默认UTF-8.
flyway.ignore-failed-future-migration当读取元数据表时是否忽略错误的迁移，默认false.
flyway.init-sqls当初始化好连接时要执行的SQL.
flyway.locations迁移脚本的位置，默认db/migration.
flyway.out-of-order是否允许无序的迁移，默认false.
flyway.password目标数据库的密码.
flyway.placeholder-prefix设置每个placeholder的前缀，默认${.
flyway.placeholder-replacementplaceholders是否要被替换，默认true.
flyway.placeholder-suffix设置每个placeholder的后缀，默认}.
flyway.placeholders.[placeholder name]设置placeholder的value
flyway.schemas设定需要flywary迁移的schema，大小写敏感，默认为连接默认的schema.
flyway.sql-migration-prefix迁移文件的前缀，默认为V.
flyway.sql-migration-separator迁移脚本的文件名分隔符，默认__
flyway.sql-migration-suffix迁移脚本的后缀，默认为.sql
flyway.tableflyway使用的元数据表名，默认为schema_version
flyway.target迁移时使用的目标版本，默认为latest version
flyway.url迁移时使用的JDBC URL，如果没有指定的话，将使用配置的主数据源
flyway.user迁移数据库的用户名
flyway.validate-on-migrate迁移时是否校验，默认为true
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ 函数式接口]]></title>
        <id>https://yz0812.github.io/post/han-shu-shi-jie-kou/</id>
        <link href="https://yz0812.github.io/post/han-shu-shi-jie-kou/">
        </link>
        <updated>2022-10-21T19:29:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="函数式接口">函数式接口</h2>
<table>
<thead>
<tr>
<th style="text-align:center"></th>
<th style="text-align:center">name</th>
<th style="text-align:center">type</th>
<th style="text-align:center">description</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">消费型接口</td>
<td style="text-align:center">Consumer</td>
<td style="text-align:center">Consumer&lt; T &gt;</td>
<td style="text-align:center">接收T对象，不返回值</td>
</tr>
<tr>
<td style="text-align:center">断定型接口</td>
<td style="text-align:center">Predicate</td>
<td style="text-align:center">Predicate&lt; T &gt;</td>
<td style="text-align:center">接收T对象并返回boolean</td>
</tr>
<tr>
<td style="text-align:center">函数型接口</td>
<td style="text-align:center">Function</td>
<td style="text-align:center">Function&lt; T, R &gt;</td>
<td style="text-align:center">接收T对象，返回R对象</td>
</tr>
<tr>
<td style="text-align:center">供给型接口</td>
<td style="text-align:center">Supplier</td>
<td style="text-align:center">Supplier&lt; T &gt;</td>
<td style="text-align:center">提供T对象（例如工厂），不接收值</td>
</tr>
</tbody>
</table>
<h3 id="consumer">Consumer</h3>
<p><strong>接收T对象，不返回值, 只有入参，没有返回值。</strong></p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/yz0812/mypic/master/202210220332371.png" alt="" loading="lazy"></figure>
<p><strong>实现一个循环打印</strong></p>
<pre><code class="language-java">
public class Java8ConsumerForEach {


    public static void main(String[] args) {
        // 消费函数定义
        Consumer&lt;String&gt; printConsumer = System.out::println;
        // 使用
        List&lt;String&gt; list = Arrays.asList(&quot;java&quot;, &quot;node&quot;, &quot;http://wuc0714.top/&quot;);
        forEach(list, printConsumer);
    }


    public static &lt;T&gt; void forEach(List&lt;T&gt; list, Consumer&lt;T&gt; consumer) {
        for (T t : list) {
            consumer.accept(t);
        }
    }
}
</code></pre>
<p><strong>有一个健身房，大家都去健身房锻炼，但是每个人锻炼的内容不一样</strong></p>
<pre><code class="language-java">package com.example.demo;

import java.math.BigDecimal;
import java.util.function.Consumer;

// 有一个健身房，大家都去健身房锻炼，但是每个人锻炼的内容不一样
public class TheGymConsumer {
    public static void main(String[] args) {
        TheGym bill = new TheGym(&quot;小李&quot;, &quot;练腿&quot;);
        theGym(bill, s -&gt; System.out.println(s.getName() + s.getContent()));

        TheGym xiaoWang = new TheGym(&quot;小王&quot;, &quot;慢跑&quot;);
        theGym(xiaoWang, s -&gt; System.out.println(s.getName() + s.getContent() + &quot;看美女&quot;));

        TheGym xiaoSun = new TheGym(&quot;小孙&quot;, &quot;慢跑&quot;);
        theGym(xiaoSun, s -&gt; {
            //如果小王也在练慢跑，那么小孙就去举重
            if (xiaoWang.getContent().equals(s.getContent())) {
                s.setContent(&quot;举重&quot;);
            }
            System.out.println(s.getName() + s.getContent());
        });

        // 小孙今天体力好，两组训练一起上
        theGym(xiaoSun
                , s -&gt; {
                    System.out.println(&quot;第一组训练：&quot; + s.getName() + s.getContent());
                }
                , s -&gt; {
                    System.out.println(&quot;第二组训练：&quot; + s.getName() + s.getContent());
                });
    }

    /**
     * @param t        t
     * @param consumer 消费者
     * @description 健身房
     * @author yz
     * @date 2022/10/22
     **/
    public static &lt;T&gt; void theGym(T t, Consumer&lt;T&gt; consumer) {
        consumer.accept(t);
    }

    /**
     * @param t         t
     * @param consumer1 消费者
     * @param consumer2 消费者
     * @description 健身房
     * @author yz
     * @date 2022/10/23
     **/
    public static &lt;T&gt; void theGym(T t, Consumer&lt;T&gt; consumer1, Consumer&lt;T&gt; consumer2) {
        consumer1.andThen(consumer2).accept(t);
    }
}

</code></pre>
<p>输出:</p>
<pre><code class="language-java">小李练腿
小王慢跑看美女
小孙举重
第一组训练：小孙举重
第二组训练：小孙举重
</code></pre>
<h3 id="predicate">Predicate</h3>
<p><strong>接收T对象并返回boolean</strong></p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/yz0812/mypic/master/202210230140559.png" alt="" loading="lazy"></figure>
<ol>
<li>test</li>
</ol>
<p><strong><code>Predicate</code> 函数接口可以用于判断一个参数是否符合某个条件。</strong></p>
<pre><code class="language-java">@Test
void test() {
    Predicate&lt;String&gt; isEmpty = String::isEmpty;
    System.out.println(isEmpty.test(&quot;&quot;));
    System.out.println(isEmpty.test(&quot;http://wuc0714.top/&quot;));
}
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">true
false
</code></pre>
<ol start="2">
<li>and</li>
</ol>
<p><strong>使用 <code>and()</code> 方法，可以让前后两个 <code>Predicate</code> 判断条件一起生效。</strong></p>
<pre><code class="language-java">    @Test
    void and(){
        List&lt;Integer&gt; numberList = Arrays.asList(3, 4, 5, 6, 7, 8, 9, 10);

        Predicate&lt;Integer&gt; greaterThan5 = number -&gt; number &gt; 5;
        Predicate&lt;Integer&gt; lessThan9 = number -&gt; number &lt; 9;
        Predicate&lt;Integer&gt; filter = greaterThan5.and(lessThan9);

        numberList = numberList.stream().filter(filter).collect(Collectors.toList());
        //相当于
        numberList = numberList.stream().filter(x -&gt; x &gt; 5 &amp;&amp; x &lt; 9).collect(Collectors.toList());
        System.out.println(numberList);
    }
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">[6, 7, 8]
</code></pre>
<ol start="3">
<li>negate</li>
</ol>
<p><strong><code>predicate.negate()</code> 方法会返回一个与指定判断相反的 <code>Predicate</code>。</strong></p>
<pre><code class="language-java">    @Test
    void negate() {
        List&lt;Integer&gt; numberList = Arrays.asList(3, 4, 5, 6, 7, 8, 9, 10);
        Predicate&lt;Integer&gt; greaterThan5 = number -&gt; number &gt; 5;
        numberList = numberList.stream().filter(greaterThan5.negate()).collect(Collectors.toList());
        System.out.println(numberList);
    }
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">[3, 4, 5]
</code></pre>
<ol start="4">
<li>or</li>
</ol>
<p><strong>使用 <code>or()</code> 方法，可以让前后两个 <code>Predicate</code> 只需要任意一个条件成立。</strong></p>
<pre><code class="language-java">    @Test
    void or() {
        List&lt;Integer&gt; numberList = Arrays.asList(3, 4, 5, 6, 7, 8, 9, 10);

        Predicate&lt;Integer&gt; greaterThan5 = number -&gt; number &gt; 5;
        Predicate&lt;Integer&gt; lessThan9 = number -&gt; number &lt; 9;
        Predicate&lt;Integer&gt; filter = greaterThan5.or(lessThan9);

        numberList = numberList.stream().filter(filter).collect(Collectors.toList());
        //相当于
        numberList = numberList.stream().filter(x -&gt; x &gt; 5 || x &lt; 9).collect(Collectors.toList());
        System.out.println(numberList);
    }
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">[3, 4, 5, 6, 7, 8, 9, 10]
</code></pre>
<ol start="5">
<li>具体使用</li>
</ol>
<p><strong>现在我们用一个案例来表示这个怎么使用</strong></p>
<pre><code class="language-java">
    @Test
    void theGym() {
        TheGym bill = new TheGym(&quot;小李&quot;, &quot;练腿&quot;);
        // 今日规则 练腿才能入会
        Predicate&lt;TheGym&gt; practiceLeg = t -&gt; t.getContent().equals(&quot;练腿&quot;);
        Boolean rule = rule(bill, practiceLeg);
        rule = rule(bill, t -&gt; t.getContent().equals(&quot;练腿&quot;));
        System.out.println(&quot;今日规则 练腿才能入会&quot; + rule);

        // 今日规则 不练腿才能入会
        rule = rule(bill, practiceLeg.negate());
        System.out.println(&quot;今日规则 不练腿才能入会&quot; + rule);

        // 今日规则 练腿和姓名叫小王才能入会
        Predicate&lt;TheGym&gt; xiaoWang = t -&gt; t.getName().equals(&quot;小王&quot;);
        rule = rule(bill, practiceLeg.and(xiaoWang));
        System.out.println(&quot;今日规则 练腿和姓名叫小王才能入会&quot; + rule);


    }
    /**
     * @param t         t
     * @param predicate 谓词
     * @return {@link Boolean }
     * @description 规则
     * @author yz
     * @date 2022/10/23
     **/
    public static &lt;T&gt; Boolean rule(T t, Predicate&lt;T&gt; predicate) {
        return predicate.test(t);
    }
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">今日规则 练腿才能入会true
今日规则 不练腿才能入会false
今日规则 练腿和姓名叫小王才能入会false
</code></pre>
<h3 id="supplier">Supplier</h3>
<p><strong><code>Supplier</code>没有入参,有返回值.所以多用于对象创建，类似于一个对象创建工厂。可以使用 <code>Lambda</code> 方式创建任意对象，也可以使用对象构造方法的方法引用创对象。</strong></p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/yz0812/mypic/master/202210230303834.png" alt="" loading="lazy"></figure>
<p><strong>示例</strong> 1：使用 <code>Supplier</code> 获取一个 1 到 10 的随机数，使用 <code>Supplier</code> 获取当前时间</p>
<pre><code class="language-java">@Test
void sample() {
    Supplier&lt;Integer&gt; supplier = () -&gt; new Random().nextInt(10);
    System.out.println(supplier.get());
    System.out.println(supplier.get());

    Supplier&lt;LocalDateTime&gt; supplier2 = LocalDateTime::now;
    System.out.println(supplier2.get());
    System.out.println(supplier2.get());
}
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">9
0
2022-10-23T02:56:01.238
2022-10-23T02:56:01.238
</code></pre>
<p><strong>示例</strong>：利用 <code>Supplier</code> 构造一个工厂模式，创建不同类别的健身房。</p>
<pre><code class="language-java">    @Test
    void factory() {
        TheGym theGym1 = theGymFactory(() -&gt; new TheGym(&quot;小李&quot;));
        TheGym theGym2 = theGymFactory(() -&gt; new TheGym(&quot;小王&quot;));
        System.out.println(theGym1);
        System.out.println(theGym2);
    }

    public static TheGym theGymFactory(Supplier&lt;? extends TheGym&gt; supplier) {
        TheGym theGym = supplier.get();
        theGym.setContent(&quot;练腿&quot;);
        return theGym;
    }
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">TheGym(name=小李, content=练腿)
TheGym(name=小王, content=练腿)
</code></pre>
<p>在 Java 8 中，为了方便 <code>Supplier</code> 的使用，提供了指定类型的 <code>Supplier</code>，有 <code>BooleanSupplier</code>, <code>DoubleSupplier</code>, <code>IntSupplier</code>, <code>LongSupplier</code>。</p>
<p>具体使用</p>
<p><strong>健身房每天搞活动，随机抽取一名学员指导健身</strong></p>
<pre><code class="language-java">@Test
void theGym() {
    // 今天抽取一个学员
    Supplier&lt;TheGym&gt; xiaoli = () -&gt; new TheGym(&quot;小李&quot;, &quot;练腰&quot;);
    exercise(xiaoli);
    // 明天抽取一个学员
    exercise(() -&gt; new TheGym(&quot;小王&quot;, &quot;长跑&quot;));
}


/**
 * @param supplier 供应商
 * @description 锻炼
 * @author yz
 * @date 2022/10/23
 **/
public static void exercise(Supplier&lt;? extends TheGym&gt; supplier) {
    TheGym theGym = supplier.get();
    System.out.println(&quot;今天指导&quot; + theGym.getName() + theGym.getContent());
}
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">今天指导小李练腰
今天指导小王长跑
</code></pre>
<h3 id="function">Function</h3>
<p><strong>接受一个指定对象,返回另外一个指定对象</strong></p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/yz0812/mypic/master/202210230315734.png" alt="" loading="lazy"></figure>
<ol>
<li>apply</li>
</ol>
<p><strong>执行给定的操作</strong></p>
<pre><code class="language-java">@Test
void apply() {
    Function&lt;String, String&gt; toUpperCase = str -&gt; str.toUpperCase();
    String result = toUpperCase.apply(&quot;http://wuc0714.top/&quot;);
    System.out.println(result);
}
</code></pre>
<ol start="2">
<li>compose</li>
</ol>
<p><strong>源码</strong></p>
<pre><code class="language-java">    default &lt;V&gt; Function&lt;V, R&gt; compose(Function&lt;? super V, ? extends T&gt; before) {
        Objects.requireNonNull(before);
        return (V v) -&gt; apply(before.apply(v));
    }
</code></pre>
<p><code>compose()</code> 方法可以让多个 Function 函数连接使用。通过源码可以分析是参数的apply的先执行</p>
<pre><code class="language-java">@Test
void compose() {
    // 操作1
    Function&lt;String, String&gt; toUpperCase = str -&gt; str.toUpperCase();
    // 操作2
    Function&lt;String, String&gt; increase = str -&gt; str + &quot;post/about/&quot;;
    // 先执行操作2，再执行操作1
    String result = toUpperCase.compose(increase).apply(&quot;http://wuc0714.top/&quot;);
    System.out.println(result);
}
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">HTTP://WUC0714.TOP/POST/ABOUT/
</code></pre>
<ol start="3">
<li>andThen</li>
</ol>
<p><strong>源码</strong></p>
<pre><code class="language-java">    default &lt;V&gt; Function&lt;T, V&gt; andThen(Function&lt;? super R, ? extends V&gt; after) {
        Objects.requireNonNull(after);
        return (T t) -&gt; after.apply(apply(t));
    }
</code></pre>
<p><code>andThen()</code> 方法可以让多个 Function 函数连接使用。通过源码可以分析是本体的apply的先执行</p>
<pre><code class="language-java">@Test
void andThen() {
    // 操作1
    Function&lt;String, String&gt; toUpperCase = str -&gt; str.toUpperCase();
    // 操作2
    Function&lt;String, String&gt; increase = str -&gt; str + &quot;post/about/&quot;;
    // 先执行操作1，再执行操作2
    String result = toUpperCase.andThen(increase).apply(&quot;http://wuc0714.top/&quot;);
    System.out.println(result);
}
</code></pre>
<p>输出结果:</p>
<pre><code class="language-java">HTTP://WUC0714.TOP/post/about/
</code></pre>
<p>在 vavr中，为了方便 <code>Function</code> 的使用，提供了指定类型的 <code>Function</code>，还有更多参数的 <code>Function1</code>,<code>Function2</code>,<code>Function3</code>,<code>Function4</code>,<code>Function5</code>,<code>Function6</code>,<code>Function7</code>,<code>Function8</code>。</p>
<p>具体使用</p>
<p>可以参考 <a href="https://wuc0714.top/post/ru-he-zai-java-zhong-ti-huan-duo-ge-if-yu-ju/#4-map%E5%87%BD%E6%95%B0%E5%BC%8F%E6%8E%A5%E5%8F%A3">如何在 Java 中替换多个 if 语句 - wuc0714</a></p>
]]></content>
    </entry>
</feed>