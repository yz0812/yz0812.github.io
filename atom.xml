<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://yz0812.top</id>
    <title>Gridea</title>
    <updated>2020-11-19T18:43:29.149Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://yz0812.top"/>
    <link rel="self" href="https://yz0812.top/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://yz0812.top/images/avatar.png</logo>
    <icon>https://yz0812.top/favicon.ico</icon>
    <rights>All rights reserved 2020, Gridea</rights>
    <entry>
        <title type="html"><![CDATA[redis 从海量数据查询出某一固定前缀的key]]></title>
        <id>https://yz0812.top/post/redis-cong-hai-liang-shu-ju-cha-xun-chu-mou-yi-gu-ding-qian-zhui-de-key/</id>
        <link href="https://yz0812.top/post/redis-cong-hai-liang-shu-ju-cha-xun-chu-mou-yi-gu-ding-qian-zhui-de-key/">
        </link>
        <updated>2020-07-08T15:35:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="redis-从海量数据查询出某一固定前缀的key">redis 从海量数据查询出某一固定前缀的key</h3>
<p>先插入1000W条数据</p>
<pre><code class="language-java">
import java.util.stream.IntStream;
import redis.clients.jedis.Jedis;
import redis.clients.jedis.Pipeline;

public class TestRedis {

  // 使用jedis实现pipeline调用
  public static void main(String[] args) {
    Jedis jedis = new Jedis(&quot;127.0.0.1&quot;, 6379);
    Pipeline pipeline = jedis.pipelined();
    long startTime = System.currentTimeMillis();
    IntStream.range(0, 10000000).forEach(it -&gt; pipeline.set(&quot;k&quot; + it, &quot;v&quot;+it ));
    pipeline.syncAndReturnAll();
    long endTime = System.currentTimeMillis();
    System.out.println(endTime - startTime);
  }
}
</code></pre>
<p>我们可以先使用dbsize 查看总共有多少数据</p>
<p>然后使用 keys k1*命令查询,查询出所有 k1前缀的数据,用时17秒.1111111条数据.</p>
<p>keys 的缺点一次性返回所有匹配的数据,key数据量过大会导致服务器卡顿,对内存消耗比较大,可能会阻塞服务器.</p>
<p>可以使用scan 命令</p>
<p>scan cursor match k1* count 10</p>
<p>基于游标的迭代器,需要基于上次一游标延续之前的迭代过程</p>
<p>cursor 游标, 以0作为游标开始一次新的迭代,知道命令返回游标0完全一次遍历.</p>
<p>不保证每次执行都会返回给定的数量,支持模糊查询</p>
<p>一次返回的数量不可控,大概率符合给定的参数</p>]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[redis 分布式锁]]></title>
        <id>https://yz0812.top/post/redis-fen-bu-shi-suo/</id>
        <link href="https://yz0812.top/post/redis-fen-bu-shi-suo/">
        </link>
        <updated>2020-07-08T15:34:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="redis-实现分布式锁">redis 实现分布式锁</h2>
<h3 id="1setnx">1.setnx</h3>
<p>使用setnx 命令创建一个值.创建之后不可以被更改,因为一直被占用</br><br>
使用expire设置设置生存时间,过期之后就会自动删除</p>
<pre><code>127.0.0.1:6379&gt; setnx locknx test
(integer) 1
127.0.0.1:6379&gt; setnx locknx t
(integer) 0
127.0.0.1:6379&gt; get locknx
&quot;test&quot;
127.0.0.1:6379&gt; expire locknx 2
(integer) 1
127.0.0.1:6379&gt; setnx locknx t
(integer) 1
127.0.0.1:6379&gt; get locknx
&quot;t&quot;
</code></pre>
<p>用java实现</p>
<pre><code class="language-java">  Jedis jedis = new Jedis(&quot;127.0.0.1&quot;, 6379);
    long status = jedis.setnx(&quot;locknx&quot;,&quot;test&quot;);
    //成功
    if (status==1){
      jedis.expire(&quot;locknx&quot;,10);
      //执行独占资源操作
    }
</code></pre>
<p>但是这种方式还是有问题如果执行到第一步时就报错了,没有释放资源,那么这个锁就会一直被占用</p>
<h3 id="2set-key-value-ex-seconds-px-milliseconds-nxxx">2.SET key value [EX seconds] [PX milliseconds] [NX|XX]</h3>
<p>EX seconds ： 将键的过期时间设置为 seconds 秒。 执行 SET key value EX seconds 的效果等同于执行 SETEX key seconds value 。</p>
<p>PX milliseconds ： 将键的过期时间设置为 milliseconds 毫秒。 执行 SET key value PX milliseconds 的效果等同于执行 PSETEX key milliseconds value 。</p>
<p>NX ： 只在键不存在时， 才对键进行设置操作。 执行 SET key value NX 的效果等同于执行 SETNX key value 。</p>
<p>XX ： 只在键已经存在时， 才对键进行设置操作。</p>
<pre><code>127.0.0.1:6379&gt; set locknx test ex 10 nx
OK
127.0.0.1:6379&gt; set locknx test ex 10 nx
(nil)
127.0.0.1:6379&gt;
</code></pre>
<p>第一次执行返回ok,然就在10秒内再执行就返回nil.<br>
java代码</p>
<pre><code class="language-java">    Jedis jedis = new Jedis(&quot;127.0.0.1&quot;, 6379);
    String result = jedis.set(&quot;locknx&quot;,&quot;test&quot;,&quot;nx&quot;,&quot;ex&quot;,10);
    //成功
    if (result.equals(&quot;ok&quot;)){
      //执行独占资源操作
    }
</code></pre>
<p>大量key同时过期的注意事项<br>
集中过期,由于清除大量的key很耗时,会出现短暂卡顿.<br>
解决办法:在设置key过期时,给每个key加上随机值.</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[redis 如何持久化]]></title>
        <id>https://yz0812.top/post/redis-ru-he-chi-jiu-hua/</id>
        <link href="https://yz0812.top/post/redis-ru-he-chi-jiu-hua/">
        </link>
        <updated>2020-07-08T15:33:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="redis-如何持久化">redis 如何持久化</h2>
<h3 id="1rdb快照-保存某个时间的全量数据快照">1.RDB(快照) 保存某个时间的全量数据快照</h3>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/yz0812/mypic/raw/master/img/redis-config.jpg" alt="图片" loading="lazy"></figure>
<p>保存策略</p>
<blockquote>
<p>save 900 1</p>
<blockquote>
<p>900秒内提交了一条数据,就产生一次快照</p>
</blockquote>
</blockquote>
<blockquote>
<p>save 300 10</p>
<blockquote>
<p>300秒内提交了10条数据,就产生一次快照</p>
</blockquote>
</blockquote>
<blockquote>
<p>save 60 10000</p>
<blockquote>
<p>60秒内提交了10000条数据,就产生一次快照</p>
</blockquote>
</blockquote>
<p>为什么定义这么多策略?<br>
应付不同的环境</p>
<blockquote>
<p>stop-writes-on-bgsave-error yes</p>
<blockquote>
<p>当备份进程报错时,就停止写入进程.保护持久化数据一致性的问题.如果自己的业务有完善的监护系统,可以关闭此选项</p>
</blockquote>
</blockquote>
<blockquote>
<p>rdbcompression yes</p>
<blockquote>
<p>再备份的时候,将rdb文件进行压缩再保存.建议设置成no,因为redis本身就属于cpu密集型服务器,再开启压缩会带来更多cpu消耗,相比硬盘成本,cpu更值钱</br><br>
如果你想要禁用RDB配置，也是非常容易的，只需要在save的最后一行写上：save &quot;&quot;</p>
</blockquote>
</blockquote>
<h3 id="2rdb快照-持久化保存某个时间的全量数据快照">2.RDB(快照) 持久化:保存某个时间的全量数据快照</h3>
<ol>
<li>save: 阻塞redis的服务器进程,知道RDB文件被创建完毕.</li>
<li>bgsave: Fork出一个子进程来创建RDB文件,不阻塞服务器进程.</li>
</ol>
<pre><code>127.0.0.1:6379&gt; save
OK
(11.68s)
127.0.0.1:6379&gt; lastsave
(integer) 1593269427
127.0.0.1:6379&gt; bgsave
Background saving started
127.0.0.1:6379&gt; lastsave
(integer) 1593269464
</code></pre>
<p>我们可以先删除 dump.rdb文件,然后再用save保存快照.可以看到阻塞了主线程11秒. lastsave可以查看备份时间.<br>
bgsave会立即返回,然后过段时间再使用lsatsave查看,可以看到两个返回的时间是不一样的.</p>
<h3 id="3自动化触发rdb持久化的方式">3.自动化触发RDB持久化的方式</h3>
<ol>
<li>根据redis.conf配置里面的 save m n定时触发(用的是besave)</li>
<li>主从复制时,主节点自动触发</li>
<li>执行Debug Reload</li>
<li>执行Shutdown 且没有k开启AOF持久化</li>
</ol>
<p>save基本不会用到,所以来看看bgsave实现原理.<br>
<img src="https://gitee.com/yz0812/mypic/raw/master/img/redis-bgsave.jpg" alt="bgsave" loading="lazy"></p>
<h2 id="aofappend-only-file持久化保存写状态">AOF(Append-Only-File)持久化,保存写状态</h2>
<ol>
<li>记录下出查询以外所有的变更数据库状态的指令</li>
<li>以append的形式追加保存到AOF文件中(增量)</li>
</ol>
<pre><code>appendonly no

# The name of the append only file (default: &quot;appendonly.aof&quot;)
appendfilename &quot;appendonly.aof&quot;

# appendfsync always
appendfsync everysec
# appendfsync no
</code></pre>
<blockquote>
<p>appendonly no</p>
<blockquote>
<p>是否开启AOF持久化</p>
</blockquote>
</blockquote>
<blockquote>
<p>appendfilename &quot;appendonly.aof&quot;</p>
<blockquote>
<p>保存的文件名</p>
</blockquote>
</blockquote>
<blockquote>
<p>appendfsync always</p>
<blockquote>
<p>一旦发生缓存区的内容发生变化就写入到aof文件</p>
</blockquote>
</blockquote>
<blockquote>
<p>appendfsync everysec</p>
<blockquote>
<p>每隔一秒钟写入到aof文件</p>
</blockquote>
</blockquote>
<blockquote>
<p>appendfsync no</p>
<blockquote>
<p>根据系统策略来保存,一般是等缓存区被填满了再保存.</p>
</blockquote>
</blockquote>
<p>RDB和AOF的优缺点<br>
RDB优点：全量数据快照，文件小，恢复快<br>
RDB缺点：无法保存最近一次快照之后的数据<br>
AOF优点：可读性高，适合保存增量数据，数据不容易丢失<br>
AOF缺点：文件大，恢复时间长<br>
RDB-AOF混合持久化方式<br>
BGSAVE做镜像全量持久化，AOF做增量持久化</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[redis特性]]></title>
        <id>https://yz0812.top/post/redis-te-xing/</id>
        <link href="https://yz0812.top/post/redis-te-xing/">
        </link>
        <updated>2020-07-08T15:21:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="redis特性">redis特性</h2>
<h3 id="1-速度快">1. 速度快</h3>
<ol>
<li>内存：数据存储在内存中,读写数据的时候都不会受到硬盘 I/O 速度的限制，所以速度极快。不同介质速度也是不同的.比如ssd,和机械速度时不同的</li>
<li>单线程：避免了不必要的上下文切换和竞争。不用考虑各种锁的问题，不存在加锁释放锁操作，不会因死锁而导致的性能消耗；</li>
<li>Redis使用I/O多路复用技术，可以处理并发的连接。redis使用非阻塞I/O；</li>
</ol>
<blockquote>
<p>redis使用多路复用技术，可以处理并发的连接。非阻塞IO 内部实现采用epoll，采用了epoll+自己实现的简单的事件框架。epoll中的读、写、关闭、连接都转化成了事件，然后利用epoll的多路复用特性，绝不在io上浪费一点时间。</p>
</blockquote>
<p>这里“多路”指的是多个网络连接，“复用”指的是复用同一个线程。</p>
<h3 id="2-redis持久化">2. redis持久化</h3>
<p>redis所有数据都保存在内存中,按理说会断电丢失数据.<br>
硬盘是具有持久化特征的,比如说电脑蓝屏时也会将数据down pull到磁盘. redis也会将数据异步的写入到磁盘.提供了RDB 和AOF两种方式.进行持久化,然后进行恢复.</p>
<h3 id="3-多种数据结构">3. 多种数据结构</h3>
<p>String 最基本的数据类型,二进制安全</p>
<p>hash  String组成的字典,适用于存储对象</p>
<p>linked List 列表按照String元素插入的有序列表</p>
<p>Sets String元素组成的无序集合,通过哈希实现,不允许重复</p>
<p>sorted Set 通过分数来为集合中的成员进行从小到大的排序,不允许重复</p>
<blockquote>
<p>特殊类型</p>
</blockquote>
<p>bitMap 位图 很小的内存高效存储数据</p>
<p>hyperLoglog超小内存唯一值计算12k,上亿或者是10亿都是12k</p>
<p>GEO 地理信息位置 经度纬度</p>
<h3 id="4-多语言">4. 多语言</h3>
<ol>
<li>java</li>
<li>python</li>
<li>php</li>
<li>lua</li>
<li>ruby</li>
<li>nodejs</li>
<li>等等</li>
</ol>
<h3 id="5-功能丰富">5. 功能丰富</h3>
<ol>
<li>消息订阅</li>
<li>lua脚本</li>
<li>事物</li>
<li>pipeline</li>
</ol>
<h3 id="6-简单">6. 简单</h3>
<ol>
<li>不依赖外部库</li>
<li>单线程模式</li>
<li>23000行代码</li>
</ol>
<h3 id="6-高可用分布式">6. 高可用，分布式</h3>
<ol>
<li>2.8版本redis-sentinel支持高可用</li>
<li>3.0版本redis-cluster支持分布式</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[stream常用方法]]></title>
        <id>https://yz0812.top/post/stream-chang-yong-fang-fa/</id>
        <link href="https://yz0812.top/post/stream-chang-yong-fang-fa/">
        </link>
        <updated>2020-07-07T17:00:29.000Z</updated>
        <summary type="html"><![CDATA[<h4 id="1-剔除重复值">1. 剔除重复值</h4>
<pre><code class="language-java">List&lt;T&gt; list = snList.stream().distinct().collect(Collectors.toList()
</code></pre>
<p>或者使用hutool的</p>
<pre><code class="language-java">collUtil.distinct()
</code></pre>
<h4 id="2-切割liststring成为字符串">2. 切割List<String>成为字符串</h4>
<pre><code class="language-java">String str = snList.stream().collect(Collectors.joining(&quot;','&quot;))
</code></pre>
<h4 id="3-将对listt进行筛选">3. 将对List<T>进行筛选</h4>
<pre><code class="language-java">List&lt;String&gt; list = list.stream().map(l -&gt; l.getStr()).collect(Collectors.toList())
</code></pre>
<h4 id="4-判断listt某个值是否存在">4. 判断List<T>某个值是否存在</h4>
<p>使用hutool的方法</p>
<pre><code class="language-java">Filter&lt;String&gt; filterCustSn =s-&gt;s.equals(&quot;张三&quot;);
if (StrUtil.isNotEmpty(CollUtil.findOne(snList,filterCustSn))){
  continue;
}
</code></pre>
<p>Stream</p>
<pre><code class="language-java">boolean bol = list.stream.filter(m-&gt;m.getStr().equals(&quot;张三&quot;)).findAny().isPresent();
</code></pre>
<h4 id="5根据条件获取listt的某个值">5.根据条件获取List<T>的某个值</h4>
<pre><code class="language-java">T t = list.stream.filter(m-&gt;m.getStr().equals(&quot;张三&quot;)).findFirst().get();
</code></pre>
<p>如果没有匹配的就会报错</p>
<pre><code class="language-java">Exception in thread &quot;main&quot; java.util.NoSuchElementException: No value present
	at java.util.Optional.get(Optional.java:135)
</code></pre>
<p>推荐使用下面的方法</p>
<pre><code class="language-java">T t = list.stream.filter(m-&gt;m.getStr().equals(&quot;张三&quot;)).findFirst().orElse(null);
</code></pre>
<h4 id="6获取listt中金额最大值-bigdecimals类型">6.获取List<T>中金额最大值 bigDecimals类型</h4>
<pre><code class="language-java">BigDecimal maxBig = Arrays.stream(bigDecimals).max(BigDecimal::compareTo).get()
</code></pre>
<h4 id="7打印">7.打印</h4>
<pre><code class="language-java">Arrays.stream(strings).forEach(System.out::println)
</code></pre>
<h4 id="8排序">8.排序</h4>
<pre><code class="language-java">List&lt;T&gt; sortList = list.stream().sorted(Comparator.comparing(T::getAge)) 
//方法二
List&lt;T&gt; sortList = Arrays.asList(strings).stream().mapToInt(Integer::parseInt).toArray();
</code></pre>
<h4 id="9查询listt所有匹配数据">9.查询List<T>所有匹配数据</h4>
<pre><code class="language-java">List&lt;T&gt; list = list.stream().filter(b-&gt;b.getStr().equals(&quot;张三&quot;)).collect(
    Collectors.joining()).trim()
</code></pre>
<h4 id="9合计-bigdecimals类型也可以使用">9.合计 bigDecimals类型也可以使用</h4>
<pre><code class="language-java">double sum= dto.stream().mapToDouble((x)-&gt;x.getMoney().doubleValue()).sum()
</code></pre>
]]></summary>
        <content type="html"><![CDATA[<h4 id="1-剔除重复值">1. 剔除重复值</h4>
<pre><code class="language-java">List&lt;T&gt; list = snList.stream().distinct().collect(Collectors.toList()
</code></pre>
<p>或者使用hutool的</p>
<pre><code class="language-java">collUtil.distinct()
</code></pre>
<h4 id="2-切割liststring成为字符串">2. 切割List<String>成为字符串</h4>
<pre><code class="language-java">String str = snList.stream().collect(Collectors.joining(&quot;','&quot;))
</code></pre>
<h4 id="3-将对listt进行筛选">3. 将对List<T>进行筛选</h4>
<pre><code class="language-java">List&lt;String&gt; list = list.stream().map(l -&gt; l.getStr()).collect(Collectors.toList())
</code></pre>
<h4 id="4-判断listt某个值是否存在">4. 判断List<T>某个值是否存在</h4>
<p>使用hutool的方法</p>
<pre><code class="language-java">Filter&lt;String&gt; filterCustSn =s-&gt;s.equals(&quot;张三&quot;);
if (StrUtil.isNotEmpty(CollUtil.findOne(snList,filterCustSn))){
  continue;
}
</code></pre>
<p>Stream</p>
<pre><code class="language-java">boolean bol = list.stream.filter(m-&gt;m.getStr().equals(&quot;张三&quot;)).findAny().isPresent();
</code></pre>
<h4 id="5根据条件获取listt的某个值">5.根据条件获取List<T>的某个值</h4>
<pre><code class="language-java">T t = list.stream.filter(m-&gt;m.getStr().equals(&quot;张三&quot;)).findFirst().get();
</code></pre>
<p>如果没有匹配的就会报错</p>
<pre><code class="language-java">Exception in thread &quot;main&quot; java.util.NoSuchElementException: No value present
	at java.util.Optional.get(Optional.java:135)
</code></pre>
<p>推荐使用下面的方法</p>
<pre><code class="language-java">T t = list.stream.filter(m-&gt;m.getStr().equals(&quot;张三&quot;)).findFirst().orElse(null);
</code></pre>
<h4 id="6获取listt中金额最大值-bigdecimals类型">6.获取List<T>中金额最大值 bigDecimals类型</h4>
<pre><code class="language-java">BigDecimal maxBig = Arrays.stream(bigDecimals).max(BigDecimal::compareTo).get()
</code></pre>
<h4 id="7打印">7.打印</h4>
<pre><code class="language-java">Arrays.stream(strings).forEach(System.out::println)
</code></pre>
<h4 id="8排序">8.排序</h4>
<pre><code class="language-java">List&lt;T&gt; sortList = list.stream().sorted(Comparator.comparing(T::getAge)) 
//方法二
List&lt;T&gt; sortList = Arrays.asList(strings).stream().mapToInt(Integer::parseInt).toArray();
</code></pre>
<h4 id="9查询listt所有匹配数据">9.查询List<T>所有匹配数据</h4>
<pre><code class="language-java">List&lt;T&gt; list = list.stream().filter(b-&gt;b.getStr().equals(&quot;张三&quot;)).collect(
    Collectors.joining()).trim()
</code></pre>
<h4 id="9合计-bigdecimals类型也可以使用">9.合计 bigDecimals类型也可以使用</h4>
<pre><code class="language-java">double sum= dto.stream().mapToDouble((x)-&gt;x.getMoney().doubleValue()).sum()
</code></pre>
<!-- more -->
<!-- more -->
<!-- more -->
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[内存溢出]]></title>
        <id>https://yz0812.top/post/nei-cun-yi-chu/</id>
        <link href="https://yz0812.top/post/nei-cun-yi-chu/">
        </link>
        <updated>2020-07-07T16:19:19.000Z</updated>
        <content type="html"><![CDATA[<p>前言：使用tomcat部署项目之后，使用poi导出大约7W条数据是报错。然后查看日志发现是内存溢出。</p>
<p>内存溢出报错信息</p>
<ol>
<li>java.lang.OutOfMemoryError: Java heap space</li>
<li>java.lang.OutOfMemoryError: PermGen space</li>
</ol>
<h3 id="解决方法">解决方法</h3>
<p>设置tomcat的内存</p>
<h4 id="1-如果是startupbat启动">1. 如果是startup.bat启动</h4>
<p>设置bin目录下的catalina.bat文件</p>
<pre><code class="language-xml">set JAVA_OPTS=%JAVA_OPTS% -server -Xms800m -Xmx800m -XX:MaxNewSize=256m -XX:PermSize=128m -XX:MaxPermSize=512m
</code></pre>
<img src="https://gitee.com/yz0812/mypic/raw/master/img/clipboard.png" width = "800" height = "200" alt="图片名称" align=center />
<p><span style="font-weight:500">网上大神提示：在JVM中如果98％的时间是用于GC且可用的Heap size 不足2％的时候将抛出此异常信息。Heap Size 最大不要超过可用物理内存的80％，一般的要将-Xms和-Xmx选项设置为相同，而-Xmn为1/4的-Xmx值。</span></p>
<h4 id="1windows服务执行方法一">1.windows服务执行方法一</h4>
<p>注册表</p>
<pre><code class="language-xml">HKEY_LOCAL_MACHINE/SOFTWARE/Wow6432Node/Apache Software Foundation/Procrun 2.0/tomcat/Parameters/Java
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/yz0812/mypic/raw/master/img/clipboard1.png" alt="catalina.bat" loading="lazy"></figure>
<h4 id="1windows服务执行方法二">1.windows服务执行方法二</h4>
<p>打开tomcat6w.exe如果打不开执行service.bat install</p>
<figure data-type="image" tabindex="2"><img src="https://gitee.com/yz0812/mypic/raw/master/img/clipboard2.png" alt="catalina.bat" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[spring事物不生效的几种情况]]></title>
        <id>https://yz0812.top/post/spring-shi-wu-bu-sheng-xiao-de-ji-chong-qing-kuang/</id>
        <link href="https://yz0812.top/post/spring-shi-wu-bu-sheng-xiao-de-ji-chong-qing-kuang/">
        </link>
        <updated>2020-07-07T13:07:24.000Z</updated>
        <content type="html"><![CDATA[<p>事务不生效的几种情况</p>
<ol>
<li>方法的访问级别不是public，这点是由spring aop决定的。不能加statis，final。</li>
<li>异常被catch住了。</li>
<li>默认情况下unchecked异常会回滚，如果是checked异常则不回滚。这个时候需要在注解上面指定异常类型 <span style="color:red">@Transactional(rollbackFor=Exception.class)</li>
</ol>
<blockquote>
<p>java里面将派生于Error或者RuntimeException（比如空指针，1/0）的异常称为unchecked异常，其他继承自java.lang.Exception得异常统称为Checked Exception，如IOException、TimeoutException等</p>
</blockquote>
<ol start="4">
<li>据库引擎要支持事务，如果是MySQL，注意表要使用支持事务的引擎，比如innodb，如果是myisam，事务是不起作用的。</li>
<li>是否开启了事物注解</li>
</ol>
<pre><code class="language-xml">    &lt;tx:annotation-driven transaction-manager=&quot;transactionManager&quot; proxy-target-class=&quot;true&quot;/&gt;
</code></pre>
<ol start="6">
<li>spring是否扫描到你这个包，如下是扫描到org.test下面的包</li>
</ol>
<pre><code class="language-xml">&lt;context:component-scan base-package=&quot;org.test&quot; &gt;&lt;/context:component-scan&gt;
</code></pre>
<ol start="7">
<li>在同一类中调用。比如a类的b方法调用了a的c方法</li>
</ol>
<h4 id="事物异常">事物异常</h4>
<pre><code class="language-java">org.springframework.transaction.NoTransactionException: No transaction aspect-managed TransactionStatus in scope
</code></pre>
<blockquote>
<p>sping的事务是通过注解配置上去的，而下面的那个方法并没有接口，在实现类里面只是一个简单的方法而已，对于事务的注解来说没有任何作用，所以在这个方法里面调用回滚的方法自然就报错了。</p>
</blockquote>
<blockquote>
<p>解决方法,因为我这是service里面调用得一个内部方法,如果我在service方法中加入事务,那么报错所有数据都会回滚,这并不是我想要得,所以我将这个方法放到另外一个serivce里面去了</p>
</blockquote>
<h4 id="手动执行回滚">手动执行回滚</h4>
<pre><code class="language-java">TransactionAspectSupport.currentTransactionStatus().setRollbackOnly();
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Sentinel验证码限流]]></title>
        <id>https://yz0812.top/post/sentinel-yan-zheng-ma-xian-liu/</id>
        <link href="https://yz0812.top/post/sentinel-yan-zheng-ma-xian-liu/">
        </link>
        <updated>2020-05-11T14:43:45.000Z</updated>
        <content type="html"><![CDATA[<pre><code class="language-java">&lt;dependency&gt;
    &lt;groupId&gt;com.alibaba.csp&lt;/groupId&gt;
    &lt;artifactId&gt;sentinel-zuul-adapter&lt;/artifactId&gt;
    &lt;version&gt;1.6.3&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>新建GatewaySentinelFilter过滤器</p>
<pre><code class="language-java">@Slf4j
@Configuration
public class GatewaySentinelFilter {

    @Bean
    public ZuulFilter sentinelZuulPreFilter() {
        return new SentinelZuulPreFilter();
    }

    @Bean
    public ZuulFilter sentinelZuulPostFilter() {
        return new SentinelZuulPostFilter();
    }

    @Bean
    public ZuulFilter sentinelZuulErrorFilter() {
        return new SentinelZuulErrorFilter();
    }

    @PostConstruct
    public void doInit() {
        initGatewayRules();
    }

    /**
     * 定义验证码请求限流，限流规则：
     *  60秒内同一个IP，同一个 key最多访问 10次
     */
    private void initGatewayRules() {
        Set&lt;ApiDefinition&gt; definitions = new HashSet&lt;&gt;();
        Set&lt;ApiPredicateItem&gt; predicateItems = new HashSet&lt;&gt;();

        predicateItems.add(new ApiPathPredicateItem().setPattern(&quot;/auth/captcha&quot;));
        ApiDefinition definition = new ApiDefinition(&quot;captcha&quot;)
                .setPredicateItems(predicateItems);
        definitions.add(definition);
        GatewayApiDefinitionManager.loadApiDefinitions(definitions);

        Set&lt;GatewayFlowRule&gt; rules = new HashSet&lt;&gt;();

        rules.add(new GatewayFlowRule(&quot;captcha&quot;)
                .setResourceMode(SentinelGatewayConstants.RESOURCE_MODE_CUSTOM_API_NAME)
                .setParamItem(
                        new GatewayParamFlowItem()
                                .setParseStrategy(SentinelGatewayConstants.PARAM_PARSE_STRATEGY_URL_PARAM)
                                .setFieldName(&quot;key&quot;)
                                .setMatchStrategy(SentinelGatewayConstants.PARAM_MATCH_STRATEGY_EXACT)
                                .setParseStrategy(SentinelGatewayConstants.PARAM_PARSE_STRATEGY_CLIENT_IP)
                )
                .setCount(10)
                .setIntervalSec(60)
        );
        GatewayRuleManager.loadRules(rules);
    }
}
</code></pre>
<p>在<code>initGatewayRules</code>方法中，我们定义了具体的限流逻辑。</p>
<p>Sentinel 1.6.0 引入了 Sentinel API Gateway Adapter Common 模块，此模块中包含网关限流的规则和自定义 API 的实体和管理逻辑：</p>
<ul>
<li><code>GatewayFlowRule</code>：网关限流规则，针对 API Gateway 的场景定制的限流规则，可以针对不同 route 或自定义的 API 分组进行限流，支持针对请求中的参数、Header、来源 IP 等进行定制化的限流。</li>
<li><code>ApiDefinition</code>：用户自定义的 API 定义分组，可以看做是一些 URL 匹配的组合。比如我们可以定义一个 API 叫 my_api，请求 path 模式为 <code>/foo/**</code> 和 <code>/baz/**</code> 的都归到 my_api 这个 API 分组下面。限流的时候可以针对这个自定义的 API 分组维度进行限流。</li>
</ul>
<p>回头看<code>initGatewayRules</code>方法，我们通过<code>ApiDefinition</code>定义了一个API分组，名称为<code>captcha</code>，匹配的URL为<code>/auth/captcha</code>；然后通过<code>GatewayFlowRule</code>指定了限流的规则。其中网关限流规则 GatewayFlowRule 的字段解释如下：</p>
<ul>
<li><code>resource</code>：资源名称，可以是网关中的 route 名称或者用户自定义的 API 分组名称。</li>
<li><code>resourceMode</code>：规则是针对 API Gateway 的 route（RESOURCE_MODE_ROUTE_ID）还是用户在 Sentinel 中定义的 API 分组（RESOURCE_MODE_CUSTOM_API_NAME），默认是 route。</li>
<li><code>grade</code>：限流指标维度，同限流规则的 grade 字段。<br>
count：限流阈值</li>
<li><code>intervalSec</code>：统计时间窗口，单位是秒，默认是 1 秒。</li>
<li><code>controlBehavior</code>：流量整形的控制效果，同限流规则的 controlBehavior 字段，目前支持快速失败和匀速排队两种模式，默认是快速失败。</li>
<li><code>burst</code>：应对突发请求时额外允许的请求数目。</li>
<li><code>maxQueueingTimeoutMs</code>：匀速排队模式下的最长排队时间，单位是毫秒，仅在匀速排队模式下生效。</li>
<li><code>paramItem</code>：参数限流配置。若不提供，则代表不针对参数进行限流，该网关规则将会被转换成普通流控规则；否则会转换成热点规则。其中的字段：</li>
</ul>
<ol>
<li><code>parseStrategy</code>：从请求中提取参数的策略，目前支持提取来源 IP（PARAM_PARSE_STRATEGY_CLIENT_IP）、Host（PARAM_PARSE_STRATEGY_HOST）、任意 Header（PARAM_PARSE_STRATEGY_HEADER）和任意 URL 参数（PARAM_PARSE_STRATEGY_URL_PARAM）四种模式。</li>
<li><code>fieldName</code>：若提取策略选择 Header 模式或 URL 参数模式，则需要指定对应的 header 名称或 URL 参数名称。</li>
<li><code>pattern</code>：参数值的匹配模式，只有匹配该模式的请求属性值会纳入统计和流控；若为空则统计该请求属性的所有值。（1.6.2 版本开始支持）</li>
<li><code>matchStrategy</code>：参数值的匹配策略，目前支持精确匹配（PARAM_MATCH_STRATEGY_EXACT）、子串匹配（PARAM_MATCH_STRATEGY_CONTAINS）和正则匹配（PARAM_MATCH_STRATEGY_REGEX）。（1.6.2 版本开始支持）</li>
</ol>
<p>用户可以通过 GatewayRuleManager.loadRules(rules) 手动加载网关规则，或通过 GatewayRuleManager.register2Property(property) 注册动态规则源动态推送（推荐方式）。</p>
<p>所以上面的限流规则为：60秒内同一个IP，同一个key最多访问10次。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Postman创建变量]]></title>
        <id>https://yz0812.top/post/postman-chuang-jian-bian-liang/</id>
        <link href="https://yz0812.top/post/postman-chuang-jian-bian-liang/">
        </link>
        <updated>2020-05-11T14:36:13.000Z</updated>
        <content type="html"><![CDATA[<p>前言在学习security中使用postman获取access_token,每次token都不一样每次都要复制太麻烦了.后找到解决方法</p>
<h3 id="1创建全局环境">1.创建全局环境</h3>
<p>点击设置按钮<br>
<img src="https://gitee.com/yz0812/mypic/raw/master/img/00671aDrgy1gc4ms2z5vkj31hc0tawgi.jpg" alt="" loading="lazy"></p>
<p>点击Globals<br>
<img src="https://gitee.com/yz0812/mypic/raw/master/img/00671aDrgy1gc4mvz7yx2j30nf0j9gm5.jpg" alt="" loading="lazy"></p>
<p>VARIABLE 变量名<br>
INITIAL VALUE 初始值<br>
CURRENT VALUE 当前值</p>
<h3 id="2创建局部环境">2.创建局部环境</h3>
<p>点击设置按钮<br>
<img src="https://gitee.com/yz0812/mypic/raw/master/img/00671aDrgy1gc4mz2hm8yj31hc0t1dhi.jpg" alt="" loading="lazy"></p>
<p>点击Add<br>
<img src="https://gitee.com/yz0812/mypic/raw/master/img/00671aDrgy1gc4n2w1sadj30np0jumxg.jpg" alt="" loading="lazy"></p>
<p>Add Environment 环境名称<br>
VARIABLE 变量名<br>
INITIAL VALUE 初始值<br>
CURRENT VALUE 当前值</p>
<p>到目前我们添加了 loca全局变量,dev局部变量 参数为access_token,refresh_token,接下来告诉大家如何使用</p>
<h3 id="3查看变量">3.查看变量</h3>
<p><img src="https://gitee.com/yz0812/mypic/raw/master/img/00671aDrgy1gc4n51ef8fj30be037743.jpg" alt="" loading="lazy"><br>
第一个 No Environment是选择局部变量<br>
第二个是查看全部变量<br>
第三个是编辑</p>
<h3 id="4赋值">4.赋值</h3>
<figure data-type="image" tabindex="1"><img src="https://gitee.com/yz0812/mypic/raw/master/img/00671aDrgy1gc4n6tkxzuj30uo0h8wfb.jpg" alt="" loading="lazy"></figure>
<pre><code class="language-javascript">pm.test(&quot;Status code is 200&quot;, function () {
    pm.response.to.have.status(200);
    let json = JSON.parse(responseBody);//声明一个变量json，将JSON返回Body里面的值解析并给变量json
    tests[&quot;a&quot;] = json.expires_in ===7200;//设置一个断言
    pm.environment.set(&quot;refresh_token&quot;, json.refresh_token);
    pm.environment.set(&quot;access_token&quot;, json.access_token);
});

</code></pre>
<h3 id="5使用变量">5.使用变量</h3>
<p>使用方法是{{变量名}}<br>
<img src="https://gitee.com/yz0812/mypic/raw/master/img/00671aDrgy1gc4n8ns9i9j30oo09odg1.jpg" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java实现多文件边压缩边下载]]></title>
        <id>https://yz0812.top/post/java-shi-xian-duo-wen-jian-bian-ya-suo-bian-xia-zai/</id>
        <link href="https://yz0812.top/post/java-shi-xian-duo-wen-jian-bian-ya-suo-bian-xia-zai/">
        </link>
        <updated>2020-05-11T14:34:42.000Z</updated>
        <content type="html"><![CDATA[<pre><code class="language-java"> //照片存放路径
  @Value(&quot;${img.url:D:/download/image/}&quot;)
  private String imgUrl;
  
  @Value(&quot;${img.suffix:.jpg}&quot;)
  private String imgSuffix;

  @Value(&quot;${csv.url:D:/download/csv/}&quot;)

public void exportEmployee(HttpServletRequest request,
      HttpServletResponse response, String employeeId) {

    String[] employeeIds = employeeId.split(&quot;,&quot;);

    //判断文件夹是否存在
    if (!FileUtil.exist(csvUrl)) {
      //创建文件夹
      FileUtil.mkdir(csvUrl);
    }

    //查询
    List&lt;EmployeeDto&gt; dtoList = employeeService.findEmployeeInId(employeeIds);
    //存储照片
    List&lt;File&gt; fileList = new ArrayList&lt;&gt;();
    //写入cvs
    List&lt;String[]&gt; csvList = new ArrayList();
    //表头
    String[] headArr = new String[]{&quot;人员编号&quot;, &quot;证件类型&quot;, &quot;人员类型&quot;, &quot;姓名&quot;, &quot;性别&quot;, &quot;出生日期&quot;, &quot;权限组名称&quot;, &quot;卡号&quot;,
        &quot;卡号有效时间&quot;, &quot;部门&quot;, &quot;国家&quot;, &quot;省份&quot;, &quot;城市&quot;, &quot;人脸图片1&quot;, &quot;特征值1&quot;, &quot;人脸图片2&quot;, &quot;特征值2&quot;, &quot;人脸图片3&quot;, &quot;特征值3&quot;};
    csvList.add(headArr);
    String[] bodyArr;

    for (EmployeeDto l : dtoList) {

      //照片存储路径  imgUrl+部门+名字+imgSuffix
      String photoUrl = imgUrl + l.getDepartmentName() + &quot;/&quot;;

      //判断文件夹是否存在
      if (!FileUtil.exist(photoUrl)) {
        //创建文件夹
        FileUtil.mkdir(photoUrl);
      }

      //判断照片是否存在
      if (!FileUtil.isDirectory(photoUrl + l.getEmployeeName() + imgSuffix)) {
        if (!ImageBase64Utils
            .saveToImgByStr(l.getPhoto(), photoUrl, l.getEmployeeName() + imgSuffix)) {
          log.error(&quot;{}图片保存本地失败&quot;, l.getPhoto());
          return;
        }
      }

      //添加照片文件
      fileList.add(new File(photoUrl + l.getEmployeeName() + imgSuffix));
      bodyArr = new String[headArr.length];
      //人员编号
      bodyArr[0] = l.getEmployeeId();
      //姓名
      bodyArr[3] = l.getEmployeeName();
      //性别
      bodyArr[4] = l.getSexId().equals(1) ? &quot;男&quot; : &quot;女&quot;;
      //出生日期
      bodyArr[5] = DateUtil.format(l.getBirthday(), &quot;yyyy-MM-dd&quot;);
      //卡号有效时间
      bodyArr[8] = &quot;2000/01/01-2037/12/31&quot;;
      //部门
      bodyArr[9] = l.getLayerorder().replaceAll(&quot;-&quot;, &quot;/&quot;);
      //人脸图片1
      bodyArr[13] = l.getEmployeeName() + imgSuffix;
      csvList.add(bodyArr);
    }
    download(csvList,request,response,fileList);
  }

  /**
   * 功能描述  创建csv文件,删除csv文件,创建zip,导出
   *
   * @param csvList 学员信息
   * @param request HttpServletRequest
   * @param response HttpServletResponse
   * @param fileList 文件列表
   * @return void
   * @author yz
   * @date 2019-08-28
   */
  public void download(List&lt;String[]&gt; csvList, HttpServletRequest request,
      HttpServletResponse response, List&lt;File&gt; fileList) {
    String csvName =
        &quot;PersonnelInformation-&quot; + DateUtil.format(new Date(), &quot;yyyyMMddhhmmsss&quot;) + &quot;.csv&quot;;

    //指定路径和编码 生成CSV
    // CSV文件本身为一种简单文本格式，有编码区分。Excel读取CSV文件中含有中文时时必须为GBK编码（Windows平台下），否则会出现乱码。
    CsvWriter writer = CsvUtil.getWriter(csvUrl + csvName, CharsetUtil.CHARSET_GBK);
    //按行写出
    writer.write(csvList);
    //关闭写入
    writer.close();
    //响应头的设置
    response.reset();
    response.setCharacterEncoding(&quot;utf-8&quot;);
    response.setContentType(&quot;multipart/form-data&quot;);

    //创建csv文件
    File csvFile = new File(csvUrl + csvName);
    if (null == csvFile) {
      log.error(&quot;csv文件不存在&quot;);
      return;
    }
    //添加到压缩列表
    fileList.add(csvFile);

    //设置压缩包的名字
    //解决不同浏览器压缩包名字含有中文时乱码的问题
    String downloadName =
        &quot;PersonnelInformation-&quot; + DateUtil.format(new Date(), &quot;yyyyMMddhhmmsss&quot;) + &quot;.zip&quot;;
    String agent = request.getHeader(&quot;USER-AGENT&quot;);
    try {
      if (agent.contains(&quot;MSIE&quot;) || agent.contains(&quot;Trident&quot;)) {
        downloadName = java.net.URLEncoder.encode(downloadName, &quot;UTF-8&quot;);
      } else {
        downloadName = new String(downloadName.getBytes(&quot;UTF-8&quot;), &quot;ISO-8859-1&quot;);
      }
    } catch (Exception e) {
      e.printStackTrace();
    }
    response.setHeader(&quot;Content-Disposition&quot;, &quot;attachment;fileName=\&quot;&quot; + downloadName + &quot;\&quot;&quot;);

    //设置压缩流：直接写入response，实现边压缩边下载
    ZipOutputStream zipos = null;
    try {
      zipos = new ZipOutputStream(new BufferedOutputStream(response.getOutputStream()));
      //设置压缩方法
      zipos.setMethod(ZipOutputStream.DEFLATED);
    } catch (Exception e) {
      log.error(&quot;设置压缩流出现异常&quot; + e.getMessage());
      e.printStackTrace();
    }

    //循环将文件写入压缩流
    DataOutputStream os = null;
    for (int i = 0; i &lt; fileList.size(); i++) {

      File file = fileList.get(i);
      try {
        //添加ZipEntry，并ZipEntry中写入文件流
        //这里，加上i是防止要下载的文件有重名的导致下载失败
        zipos.putNextEntry(new ZipEntry(file.getName()));
        os = new DataOutputStream(zipos);
        InputStream is = new FileInputStream(file);
        byte[] b = new byte[100];
        int length;
        while ((length = is.read(b)) != -1) {
          os.write(b, 0, length);
        }
        is.close();
        zipos.closeEntry();
      } catch (IOException e) {
        log.error(&quot;循环将文件写入压缩流出现异常&quot; + e.getMessage());
        e.printStackTrace();
      }
    }

    //关闭流
    try {
      os.flush();
      os.close();
      zipos.close();
    } catch (IOException e) {
      log.error(&quot;关闭流现异常&quot; + e.getMessage());
      e.printStackTrace();
    }

    //删除生成的csv
    if (!FileUtil.del(csvUrl + csvName)) {
      log.error(&quot;删除{}文件失败&quot;, csvName);
    }
  }

  /**
   * 功能描述 二进制转base64
   *
   * @param dtoList 查询出来的数据列表
   * @return java.util.List&lt;com.yczn.dto.EmployeeDto&gt;
   * @author yz
   * @date 2019-08-27
   */
  private List&lt;EmployeeDto&gt; imgToString(List&lt;EmployeeDto&gt; dtoList) {
    for (EmployeeDto employeeDto : dtoList) {

      Encoder encoder = Base64.getEncoder();

      employeeDto.setPhotoBase64(
          employeeDto.getPhoto() != null ? encoder.encodeToString(employeeDto.getPhoto()) : &quot;&quot;);
    }
    return dtoList;
  }
</code></pre>
]]></content>
    </entry>
</feed>